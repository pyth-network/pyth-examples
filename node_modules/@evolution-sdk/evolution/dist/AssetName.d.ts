import { Equal, FastCheck, Hash, Inspectable, Schema } from "effect";
declare const AssetName_base: Schema.TaggedClass<AssetName, "AssetName", {
    readonly _tag: Schema.tag<"AssetName">;
} & {
    bytes: Schema.filter<Schema.Schema<Uint8Array<ArrayBufferLike>, string, never>>;
}>;
/**
 * Schema for AssetName representing a native asset identifier.
 * Asset names are limited to 32 bytes (0-64 hex characters).
 *
 * @since 2.0.0
 * @category model
 */
export declare class AssetName extends AssetName_base {
    /**
     * Convert to JSON representation.
     *
     * @since 2.0.0
     * @category conversions
     */
    toJSON(): {
        _tag: string;
        bytes: string;
    };
    /**
     * Convert to string representation.
     *
     * @since 2.0.0
     * @category conversions
     */
    toString(): string;
    /**
     * Custom inspect for Node.js REPL.
     *
     * @since 2.0.0
     * @category conversions
     */
    [Inspectable.NodeInspectSymbol](): unknown;
    /**
     * Structural equality check.
     *
     * @since 2.0.0
     * @category equality
     */
    [Equal.symbol](that: unknown): boolean;
    /**
     * Content-based hash for optimization of Equal.equals.
     *
     * @since 2.0.0
     * @category hashing
     */
    [Hash.symbol](): number;
}
/**
 * Schema for encoding/decoding AssetName as bytes.
 *
 * @since 2.0.0
 * @category schemas
 */
export declare const FromBytes: Schema.transform<Schema.SchemaClass<Uint8Array<ArrayBufferLike>, Uint8Array<ArrayBufferLike>, never>, Schema.SchemaClass<AssetName, AssetName, never>>;
/**
 * Schema for encoding/decoding AssetName as hex strings.
 *
 * @since 2.0.0
 * @category schemas
 */
export declare const FromHex: Schema.transform<Schema.filter<Schema.Schema<Uint8Array<ArrayBufferLike>, string, never>>, Schema.transform<Schema.SchemaClass<Uint8Array<ArrayBufferLike>, Uint8Array<ArrayBufferLike>, never>, Schema.SchemaClass<AssetName, AssetName, never>>>;
/**
 * Check if the given value is a valid AssetName
 *
 * @since 2.0.0
 * @category predicates
 */
export declare const isAssetName: (u: unknown, overrideOptions?: import("effect/SchemaAST").ParseOptions | number) => u is AssetName;
/**
 * FastCheck arbitrary for generating random AssetName instances.
 *
 * @since 2.0.0
 * @category arbitrary
 */
export declare const arbitrary: FastCheck.Arbitrary<AssetName>;
/**
 * Parse AssetName from bytes.
 *
 * @since 2.0.0
 * @category parsing
 */
export declare const fromBytes: (bytes: Uint8Array) => AssetName;
/**
 * Parse AssetName from hex string.
 *
 * @since 2.0.0
 * @category parsing
 */
export declare const fromHex: (hex: string) => AssetName;
/**
 * Encode AssetName to bytes.
 *
 * @since 2.0.0
 * @category encoding
 */
export declare const toBytes: (assetName: AssetName) => Uint8Array<ArrayBufferLike>;
/**
 * Encode AssetName to hex string.
 *
 * @since 2.0.0
 * @category encoding
 */
export declare const toHex: (assetName: AssetName) => string;
export {};
//# sourceMappingURL=AssetName.d.ts.map