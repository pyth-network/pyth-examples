import { Equal, FastCheck, Hash, Inspectable, Schema } from "effect";
declare const Pointer_base: Schema.TaggedClass<Pointer, "Pointer", {
    readonly _tag: Schema.tag<"Pointer">;
} & {
    slot: Schema.refine<number, typeof Schema.Number>;
    txIndex: Schema.refine<number, typeof Schema.Number>;
    certIndex: Schema.refine<number, typeof Schema.Number>;
}>;
/**
 * Schema for pointer to a stake registration certificate
 * Contains slot, transaction index, and certificate index information
 *
 * @since 2.0.0
 * @category schemas
 */
export declare class Pointer extends Pointer_base {
    toJSON(): {
        _tag: "Pointer";
        slot: number;
        txIndex: number;
        certIndex: number;
    };
    toString(): string;
    [Inspectable.NodeInspectSymbol](): unknown;
    [Equal.symbol](that: unknown): boolean;
    [Hash.symbol](): number;
}
/**
 * Check if the given value is a valid Pointer
 *
 *
 * @since 2.0.0
 * @category predicates
 */
export declare const isPointer: (u: unknown, overrideOptions?: import("effect/SchemaAST").ParseOptions | number) => u is Pointer;
/**
 * FastCheck arbitrary for generating random Pointer instances
 *
 * @since 2.0.0
 * @category generators
 */
export declare const arbitrary: FastCheck.Arbitrary<Pointer>;
export {};
//# sourceMappingURL=Pointer.d.ts.map