/**
 * Convert UnixTime (milliseconds) to seconds.
 *
 * @param unixTime - Unix time in milliseconds
 * @returns Unix time in seconds
 *
 * @category Time
 * @since 2.0.0
 */
export const toSeconds = unixTime => unixTime / 1000n;
/**
 * Convert seconds to UnixTime (milliseconds).
 *
 * @param seconds - Time in seconds
 * @returns Unix time in milliseconds
 *
 * @category Time
 * @since 2.0.0
 */
export const fromSeconds = seconds => seconds * 1000n;
/**
 * Get current UnixTime.
 *
 * @returns Current Unix time in milliseconds
 *
 * @category Time
 * @since 2.0.0
 */
export const now = () => BigInt(Date.now());
/**
 * Convert JavaScript Date to UnixTime.
 *
 * @param date - JavaScript Date object
 * @returns Unix time in milliseconds
 *
 * @category Time
 * @since 2.0.0
 */
export const fromDate = date => BigInt(date.getTime());
/**
 * Convert UnixTime to JavaScript Date.
 *
 * @param unixTime - Unix time in milliseconds
 * @returns JavaScript Date object
 *
 * @category Time
 * @since 2.0.0
 */
export const toDate = unixTime => new Date(Number(unixTime));
//# sourceMappingURL=UnixTime.js.map