import { Equal, Hash, Inspectable, Schema } from "effect";
import * as PlutusData from "./Data.js";
declare const InlineDatum_base: Schema.TaggedClass<InlineDatum, "InlineDatum", {
    readonly _tag: Schema.tag<"InlineDatum">;
} & {
    data: PlutusData.DataSchema;
}>;
/**
 * Schema for InlineDatum representing inline plutus data embedded directly in the transaction output.
 *
 * @since 2.0.0
 * @category schemas
 */
export declare class InlineDatum extends InlineDatum_base {
    /**
     * @since 2.0.0
     * @category json
     */
    toJSON(): {
        _tag: "InlineDatum";
        data: PlutusData.Data;
    };
    /**
     * @since 2.0.0
     * @category string
     */
    toString(): string;
    /**
     * @since 2.0.0
     * @category inspect
     */
    [Inspectable.NodeInspectSymbol](): unknown;
    /**
     * @since 2.0.0
     * @category equality
     */
    [Equal.symbol](that: unknown): boolean;
    /**
     * @since 2.0.0
     * @category hash
     */
    [Hash.symbol](): number;
}
/**
 * FastCheck arbitrary for generating random InlineDatum instances.
 *
 * @since 2.0.0
 * @category testing
 */
export declare const arbitrary: import("effect/FastCheck").Arbitrary<InlineDatum>;
/**
 * Type guard to check if a value is an InlineDatum.
 *
 * @since 2.0.0
 * @category predicates
 */
export declare const isInlineDatum: (u: unknown, overrideOptions?: import("effect/SchemaAST").ParseOptions | number) => u is InlineDatum;
export {};
//# sourceMappingURL=InlineDatum.d.ts.map