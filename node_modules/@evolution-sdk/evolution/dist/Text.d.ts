import { FastCheck, Schema } from "effect";
export declare const Text: typeof Schema.String;
/**
 * Schema for converting between strings and bytes (UTF-8).
 *
 * ```
 * text <-> bytes
 * ```
 *
 * @since 2.0.0
 * @category schemas
 */
export declare const FromBytes: Schema.transform<typeof Schema.Uint8ArrayFromSelf, typeof Schema.String>;
/**
 * Schema for converting between strings and hex representation of UTF-8 bytes.
 *
 * ```
 * text <-> hex
 * ```
 *
 * @since 2.0.0
 * @category schemas
 */
export declare const FromHex: Schema.transform<Schema.Schema<Uint8Array<ArrayBufferLike>, string, never>, typeof Schema.String>;
/**
 * Creates a schema that validates text length equals a specific value.
 *
 * @since 2.0.0
 * @category validation
 */
export declare const length: (length: number, identifier?: string) => (self: Schema.Schema.Any) => Schema.filter<Schema.Schema.Any>;
/**
 * Creates a curried filter that validates text length is within a range.
 * Preserves Context inference from the base schema.
 *
 * @since 2.0.0
 * @category composition
 */
export declare const between: (min: number, max: number, moduleName: string) => <S extends Schema.Schema<any, string>>(baseSchema: S) => Schema.filter<S>;
/**
 * Creates a schema that validates text length is at least min.
 *
 * @since 2.0.0
 * @category validation
 */
export declare const min: (min: number, identifier?: string) => (self: Schema.Schema.Any) => Schema.filter<Schema.Schema.Any>;
/**
 * Creates a schema that validates text length is at most max.
 *
 * @since 2.0.0
 * @category validation
 */
export declare const max: (max: number, identifier?: string) => (self: Schema.Schema.Any) => Schema.filter<Schema.Schema.Any>;
/**
 * FastCheck arbitrary for generating random text strings
 *
 * @since 2.0.0
 * @category testing
 */
export declare const arbitrary: FastCheck.Arbitrary<string>;
/**
 * Convert bytes to text
 *
 * @since 2.0.0
 * @category conversion
 */
export declare const fromBytes: (i: Uint8Array<ArrayBufferLike>, overrideOptions?: import("effect/SchemaAST").ParseOptions) => string;
/**
 * Convert hex string to text
 *
 * @since 2.0.0
 * @category conversion
 */
export declare const fromHex: (i: string, overrideOptions?: import("effect/SchemaAST").ParseOptions) => string;
/**
 * Convert text to bytes
 *
 * @since 2.0.0
 * @category conversion
 */
export declare const toBytes: (a: string, overrideOptions?: import("effect/SchemaAST").ParseOptions) => Uint8Array<ArrayBufferLike>;
/**
 * Convert text to hex string
 *
 * @since 2.0.0
 * @category conversion
 */
export declare const toHex: (a: string, overrideOptions?: import("effect/SchemaAST").ParseOptions) => string;
//# sourceMappingURL=Text.d.ts.map