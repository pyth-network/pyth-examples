import { Effect as Eff, Equal, FastCheck, Hash, Inspectable, ParseResult, Schema } from "effect";
import * as Anchor from "./Anchor.js";
import * as CBOR from "./CBOR.js";
import * as Credential from "./Credential.js";
import * as DRep from "./DRep.js";
import * as GovernanceAction from "./GovernanceAction.js";
import * as KeyHash from "./KeyHash.js";
import * as PoolKeyHash from "./PoolKeyHash.js";
import * as ScriptHash from "./ScriptHash.js";
import * as TransactionHash from "./TransactionHash.js";
import * as TransactionIndex from "./TransactionIndex.js";
/**
 * Helper function for content-based Map equality using Equal.equals.
 * Compares two Maps by iterating entries and using Equal.equals for both keys and values.
 *
 * @since 2.0.0
 * @category equality
 */
const mapEquals = (a, b) => {
  if (a.size !== b.size) return false;
  for (const [aKey, aValue] of a.entries()) {
    let found = false;
    for (const [bKey, bValue] of b.entries()) {
      if (Equal.equals(aKey, bKey)) {
        if (aValue instanceof Map && bValue instanceof Map) {
          if (!mapEquals(aValue, bValue)) return false;
        } else {
          if (!Equal.equals(aValue, bValue)) return false;
        }
        found = true;
        break;
      }
    }
    if (!found) return false;
  }
  return true;
};
/**
 * Helper function for content-based Map hashing.
 * Computes hash by XORing hashes of all entries for order-independence.
 *
 * @since 2.0.0
 * @category hashing
 */
const mapHash = map => {
  let hash = Hash.hash(map.size);
  for (const [key, value] of map.entries()) {
    hash ^= Hash.hash(key) ^ Hash.hash(value);
  }
  return hash;
};
/**
 * Voter types based on Conway CDDL specification.
 *
 * Conway / CML mapping:
 *  - [0, addr_keyhash]   ConstitutionalCommitteeHotKeyHash
 *  - [1, script_hash]    ConstitutionalCommitteeHotScriptHash
 *  - [2, addr_keyhash]   DRepKeyHash
 *  - [3, script_hash]    DRepScriptHash
 *  - [4, pool_keyhash]   StakingPoolKeyHash
 *
 * @since 2.0.0
 * @category schemas
 */
export class ConstitutionalCommitteeVoter extends /*#__PURE__*/Schema.TaggedClass()("ConstitutionalCommitteeVoter", {
  credential: Credential.Credential
}) {
  toJSON() {
    return {
      _tag: "ConstitutionalCommitteeVoter",
      credential: this.credential
    };
  }
  toString() {
    return Inspectable.format(this.toJSON());
  }
  [Inspectable.NodeInspectSymbol]() {
    return this.toJSON();
  }
  [Equal.symbol](that) {
    return that instanceof ConstitutionalCommitteeVoter && Equal.equals(this.credential, that.credential);
  }
  [Hash.symbol]() {
    return Hash.cached(this, Hash.hash(this.credential));
  }
}
export class DRepVoter extends /*#__PURE__*/Schema.TaggedClass()("DRepVoter", {
  drep: DRep.DRep
}) {
  toJSON() {
    return {
      _tag: "DRepVoter",
      drep: this.drep
    };
  }
  toString() {
    return Inspectable.format(this.toJSON());
  }
  [Inspectable.NodeInspectSymbol]() {
    return this.toJSON();
  }
  [Equal.symbol](that) {
    return that instanceof DRepVoter && Equal.equals(this.drep, that.drep);
  }
  [Hash.symbol]() {
    return Hash.cached(this, Hash.hash(this.drep));
  }
}
export class StakePoolVoter extends /*#__PURE__*/Schema.TaggedClass()("StakePoolVoter", {
  poolKeyHash: PoolKeyHash.PoolKeyHash
}) {
  toJSON() {
    return {
      _tag: "StakePoolVoter",
      poolKeyHash: this.poolKeyHash
    };
  }
  toString() {
    return Inspectable.format(this.toJSON());
  }
  [Inspectable.NodeInspectSymbol]() {
    return this.toJSON();
  }
  [Equal.symbol](that) {
    return that instanceof StakePoolVoter && Equal.equals(this.poolKeyHash, that.poolKeyHash);
  }
  [Hash.symbol]() {
    return Hash.cached(this, Hash.hash(this.poolKeyHash));
  }
}
/**
 * Voter union schema.
 *
 * @since 2.0.0
 * @category schemas
 */
export const Voter = /*#__PURE__*/Schema.Union(ConstitutionalCommitteeVoter, DRepVoter, StakePoolVoter);
/**
 * CDDL schema for Voter as tuple structure.
 * ```
 * Maps to: [voter_type, voter_data]
 * ```
 *
 * @since 2.0.0
 * @category schemas
 */
// Match CML: split by concrete key/script variants for committee and drep, plus pool keyhash
// 0 = ConstitutionalCommitteeHotKeyHash (addr_keyhash)
// 1 = ConstitutionalCommitteeHotScriptHash (script_hash)
// 2 = DRepKeyHash (addr_keyhash)
// 3 = DRepScriptHash (script_hash)
// 4 = StakingPoolKeyHash (pool_keyhash)
export const VoterCDDL = /*#__PURE__*/Schema.Union(/*#__PURE__*/Schema.Tuple(/*#__PURE__*/Schema.Literal(0n), CBOR.ByteArray), /*#__PURE__*/Schema.Tuple(/*#__PURE__*/Schema.Literal(1n), CBOR.ByteArray), /*#__PURE__*/Schema.Tuple(/*#__PURE__*/Schema.Literal(2n), CBOR.ByteArray), /*#__PURE__*/Schema.Tuple(/*#__PURE__*/Schema.Literal(3n), CBOR.ByteArray), /*#__PURE__*/Schema.Tuple(/*#__PURE__*/Schema.Literal(4n), CBOR.ByteArray));
/**
 * CDDL transformation schema for Voter.
 *
 * @since 2.0.0
 * @category schemas
 */
export const VoterFromCDDL = /*#__PURE__*/Schema.transformOrFail(VoterCDDL, /*#__PURE__*/Schema.typeSchema(Voter), {
  strict: true,
  encode: voter => Eff.gen(function* () {
    switch (voter._tag) {
      case "ConstitutionalCommitteeVoter":
        {
          if (voter.credential._tag === "KeyHash") {
            return [0n, voter.credential.hash];
          } else {
            return [1n, voter.credential.hash];
          }
        }
      case "DRepVoter":
        {
          if (voter.drep._tag === "KeyHashDRep") {
            return [2n, voter.drep.keyHash.hash];
          } else if (voter.drep._tag === "ScriptHashDRep") {
            return [3n, voter.drep.scriptHash.hash];
          } else {
            return yield* ParseResult.fail(new ParseResult.Type(VoterCDDL.ast, voter, "Always* DRep variants are not valid Voter identifiers"));
          }
        }
      case "StakePoolVoter":
        {
          return [4n, voter.poolKeyHash.hash];
        }
    }
  }),
  decode: cddl => Eff.gen(function* () {
    const [voterType, voterData] = cddl;
    switch (voterType) {
      case 0n:
        {
          const keyHash = yield* ParseResult.decode(KeyHash.FromBytes)(voterData);
          return new ConstitutionalCommitteeVoter({
            credential: keyHash
          });
        }
      case 1n:
        {
          const scriptHash = yield* ParseResult.decode(ScriptHash.FromBytes)(voterData);
          return new ConstitutionalCommitteeVoter({
            credential: scriptHash
          });
        }
      case 2n:
        {
          const keyHash = yield* ParseResult.decode(KeyHash.FromBytes)(voterData);
          return new DRepVoter({
            drep: new DRep.KeyHashDRep({
              keyHash
            })
          });
        }
      case 3n:
        {
          const scriptHash = yield* ParseResult.decode(ScriptHash.FromBytes)(voterData);
          return new DRepVoter({
            drep: new DRep.ScriptHashDRep({
              scriptHash
            })
          });
        }
      case 4n:
        {
          const poolKeyHash = yield* ParseResult.decode(PoolKeyHash.FromBytes)(voterData);
          return new StakePoolVoter({
            poolKeyHash
          });
        }
      default:
        return yield* ParseResult.fail(new ParseResult.Type(VoterCDDL.ast, cddl));
    }
  })
});
/**
 * Vote types based on Conway CDDL specification.
 *
 * ```
 * vote = 0 / 1 / 2  ; No / Yes / Abstain
 * ```
 *
 * @since 2.0.0
 * @category schemas
 */
export class NoVote extends /*#__PURE__*/Schema.TaggedClass()("NoVote", {}) {
  toJSON() {
    return {
      _tag: "NoVote"
    };
  }
  toString() {
    return Inspectable.format(this.toJSON());
  }
  [Inspectable.NodeInspectSymbol]() {
    return this.toJSON();
  }
  [Equal.symbol](that) {
    return that instanceof NoVote;
  }
  [Hash.symbol]() {
    return Hash.cached(this, Hash.string("NoVote"));
  }
}
export class YesVote extends /*#__PURE__*/Schema.TaggedClass()("YesVote", {}) {
  toJSON() {
    return {
      _tag: "YesVote"
    };
  }
  toString() {
    return Inspectable.format(this.toJSON());
  }
  [Inspectable.NodeInspectSymbol]() {
    return this.toJSON();
  }
  [Equal.symbol](that) {
    return that instanceof YesVote;
  }
  [Hash.symbol]() {
    return Hash.cached(this, Hash.string("YesVote"));
  }
}
export class AbstainVote extends /*#__PURE__*/Schema.TaggedClass()("AbstainVote", {}) {
  toJSON() {
    return {
      _tag: "AbstainVote"
    };
  }
  toString() {
    return Inspectable.format(this.toJSON());
  }
  [Inspectable.NodeInspectSymbol]() {
    return this.toJSON();
  }
  [Equal.symbol](that) {
    return that instanceof AbstainVote;
  }
  [Hash.symbol]() {
    return Hash.cached(this, Hash.string("AbstainVote"));
  }
}
/**
 * Vote union schema.
 *
 * @since 2.0.0
 * @category schemas
 */
export const Vote = /*#__PURE__*/Schema.Union(NoVote, YesVote, AbstainVote);
/**
 * CDDL schema for Vote.
 *
 * @since 2.0.0
 * @category schemas
 */
export const VoteCDDL = /*#__PURE__*/Schema.Union(/*#__PURE__*/Schema.Literal(0n),
/*#__PURE__*/
// No
Schema.Literal(1n),
/*#__PURE__*/
// Yes
Schema.Literal(2n) // Abstain
);
/**
 * CDDL transformation schema for Vote.
 *
 * @since 2.0.0
 * @category schemas
 */
export const VoteFromCDDL = /*#__PURE__*/Schema.transformOrFail(VoteCDDL, /*#__PURE__*/Schema.typeSchema(Vote), {
  strict: true,
  encode: vote => Eff.gen(function* () {
    switch (vote._tag) {
      case "NoVote":
        return 0n;
      case "YesVote":
        return 1n;
      case "AbstainVote":
        return 2n;
    }
  }),
  decode: cddl => Eff.gen(function* () {
    switch (cddl) {
      case 0n:
        return new NoVote();
      case 1n:
        return new YesVote();
      case 2n:
        return new AbstainVote();
      default:
        return yield* ParseResult.fail(new ParseResult.Type(VoteCDDL.ast, cddl));
    }
  })
});
/**
 * Voting procedure based on Conway CDDL specification.
 *
 * ```
 * voting_procedure = [ vote, anchor / null ]
 * ```
 *
 * @since 2.0.0
 * @category schemas
 */
export class VotingProcedure extends /*#__PURE__*/Schema.Class("VotingProcedure")({
  vote: Vote,
  anchor: /*#__PURE__*/Schema.NullOr(Anchor.Anchor)
}) {
  toJSON() {
    return {
      vote: this.vote,
      anchor: this.anchor
    };
  }
  toString() {
    return Inspectable.format(this.toJSON());
  }
  [Inspectable.NodeInspectSymbol]() {
    return this.toJSON();
  }
  [Equal.symbol](that) {
    return that instanceof VotingProcedure && Equal.equals(this.vote, that.vote) && Equal.equals(this.anchor, that.anchor);
  }
  [Hash.symbol]() {
    return Hash.cached(this, Hash.combine(Hash.hash(this.vote))(Hash.hash(this.anchor)));
  }
}
/**
 * CDDL schema for VotingProcedure tuple structure.
 *
 * @since 2.0.0
 * @category schemas
 */
export const VotingProcedureCDDL = /*#__PURE__*/Schema.Tuple(VoteCDDL,
/*#__PURE__*/
// vote
Schema.NullOr(Anchor.CDDLSchema) // anchor / null
);
/**
 * CDDL transformation schema for VotingProcedure.
 *
 * @since 2.0.0
 * @category schemas
 */
export const VotingProcedureFromCDDL = /*#__PURE__*/Schema.transformOrFail(VotingProcedureCDDL, /*#__PURE__*/Schema.typeSchema(VotingProcedure), {
  strict: true,
  encode: procedure => Eff.gen(function* () {
    const voteCDDL = yield* ParseResult.encode(VoteFromCDDL)(procedure.vote);
    const anchorCDDL = procedure.anchor ? yield* ParseResult.encode(Anchor.FromCDDL)(procedure.anchor) : null;
    return [voteCDDL, anchorCDDL];
  }),
  decode: ([voteCDDL, anchorCDDL]) => Eff.gen(function* () {
    const vote = yield* ParseResult.decode(VoteFromCDDL)(voteCDDL);
    const anchor = anchorCDDL ? yield* ParseResult.decode(Anchor.FromCDDL)(anchorCDDL) : null;
    return new VotingProcedure({
      vote,
      anchor
    });
  })
});
/**
 * VotingProcedures based on Conway CDDL specification.
 *
 * ```
 * voting_procedures = {+ voter => {+ gov_action_id => voting_procedure}}
 * ```
 *
 * A nested map structure where voters map to their votes on specific governance actions.
 *
 * @since 2.0.0
 * @category model
 */
export class VotingProcedures extends /*#__PURE__*/Schema.Class("VotingProcedures")({
  procedures: /*#__PURE__*/Schema.Map({
    key: Voter,
    value: /*#__PURE__*/Schema.Map({
      key: GovernanceAction.GovActionId,
      value: VotingProcedure
    })
  })
}) {
  toJSON() {
    const serialized = [];
    for (const [voter, actionMap] of this.procedures.entries()) {
      const voterJson = voter.toJSON();
      const actions = [];
      for (const [govActionId, procedure] of actionMap.entries()) {
        const actionIdJson = govActionId.toJSON();
        actions.push([actionIdJson, procedure.toJSON()]);
      }
      serialized.push([voterJson, actions]);
    }
    return {
      procedures: serialized
    };
  }
  toString() {
    return Inspectable.format(this.toJSON());
  }
  [Inspectable.NodeInspectSymbol]() {
    return this.toJSON();
  }
  [Equal.symbol](that) {
    return that instanceof VotingProcedures && mapEquals(this.procedures, that.procedures);
  }
  /**
   * @since 2.0.0
   * @category hashing
   */
  [Hash.symbol]() {
    let hash = Hash.hash(this.procedures.size);
    for (const [voter, govActionMap] of this.procedures.entries()) {
      const voterHash = Hash.hash(voter);
      const govActionMapHash = mapHash(govActionMap);
      hash ^= voterHash ^ govActionMapHash;
    }
    return Hash.cached(this, hash);
  }
}
/**
 * CDDL schema for VotingProcedures map structure.
 *
 * @since 2.0.0
 * @category schemas
 */
export const CDDLSchema = /*#__PURE__*/Schema.MapFromSelf({
  key: VoterCDDL,
  // voter
  value: /*#__PURE__*/Schema.MapFromSelf({
    key: GovernanceAction.GovActionIdCDDL,
    // gov_action_id
    value: VotingProcedureCDDL // voting_procedure
  })
});
/**
 * CDDL transformation schema for VotingProcedures.
 *
 * @since 2.0.0
 * @category schemas
 */
export const FromCDDL = /*#__PURE__*/Schema.transformOrFail(CDDLSchema, /*#__PURE__*/Schema.typeSchema(VotingProcedures), {
  strict: true,
  encode: toA => Eff.gen(function* () {
    const mapEntries = new Map();
    for (const [voter, govActionMap] of toA.procedures) {
      const voterCDDL = yield* ParseResult.encode(VoterFromCDDL)(voter);
      const innerMapEntries = new Map();
      for (const [govActionId, votingProcedure] of govActionMap) {
        const govActionIdCDDL = yield* ParseResult.encode(GovernanceAction.GovActionIdFromCDDL)(govActionId);
        const procedureCDDL = yield* ParseResult.encode(VotingProcedureFromCDDL)(votingProcedure);
        innerMapEntries.set(govActionIdCDDL, procedureCDDL);
      }
      mapEntries.set(voterCDDL, innerMapEntries);
    }
    return mapEntries;
  }),
  decode: fromA => Eff.gen(function* () {
    const proceduresMap = new Map();
    for (const [voterCDDL, innerMapCDDL] of fromA) {
      const voter = yield* ParseResult.decode(VoterFromCDDL)(voterCDDL);
      const govActionMap = new Map();
      for (const [govActionIdCDDL, procedureCDDL] of innerMapCDDL) {
        const govActionId = yield* ParseResult.decode(GovernanceAction.GovActionIdFromCDDL)(govActionIdCDDL);
        const procedure = yield* ParseResult.decode(VotingProcedureFromCDDL)(procedureCDDL);
        govActionMap.set(govActionId, procedure);
      }
      proceduresMap.set(voter, govActionMap);
    }
    return new VotingProcedures({
      procedures: proceduresMap
    });
  })
});
/**
 * CBOR bytes transformation schema for VotingProcedures.
 *
 * @since 2.0.0
 * @category schemas
 */
export const FromCBORBytes = (options = CBOR.CML_DEFAULT_OPTIONS) => Schema.compose(CBOR.FromBytes(options),
// Uint8Array → CBOR
FromCDDL // CBOR → VotingProcedures
).annotations({
  identifier: "VotingProcedures.FromCBORBytes",
  title: "VotingProcedures from CBOR Bytes",
  description: "Transforms CBOR bytes to VotingProcedures"
});
/**
 * CBOR hex transformation schema for VotingProcedures.
 *
 * @since 2.0.0
 * @category schemas
 */
export const FromCBORHex = (options = CBOR.CML_DEFAULT_OPTIONS) => Schema.compose(Schema.Uint8ArrayFromHex,
// string → Uint8Array
FromCBORBytes(options) // Uint8Array → VotingProcedures
).annotations({
  identifier: "VotingProcedures.FromCBORHex",
  title: "VotingProcedures from CBOR Hex",
  description: "Transforms CBOR hex string to VotingProcedures"
});
// ============================================================================
// Constructors
// ============================================================================
// Constructors - Simple helper functions
// ============================================================================
/**
 * Create a No vote.
 *
 * @since 2.0.0
 * @category constructors
 */
export const no = () => new NoVote();
/**
 * Create a Yes vote.
 *
 * @since 2.0.0
 * @category constructors
 */
export const yes = () => new YesVote();
/**
 * Create an Abstain vote.
 *
 * @since 2.0.0
 * @category constructors
 */
export const abstain = () => new AbstainVote();
// ============================================================================
// Type Guards
// ============================================================================
/**
 * Check if a voter is a Constitutional Committee voter.
 *
 * @since 2.0.0
 * @category predicates
 */
export const isConstitutionalCommitteeVoter = voter => voter._tag === "ConstitutionalCommitteeVoter";
/**
 * Check if a voter is a DRep voter.
 *
 * @since 2.0.0
 * @category predicates
 */
export const isDRepVoter = voter => voter._tag === "DRepVoter";
/**
 * Check if a voter is a Stake Pool voter.
 *
 * @since 2.0.0
 * @category predicates
 */
export const isStakePoolVoter = voter => voter._tag === "StakePoolVoter";
/**
 * Check if a vote is a No vote.
 *
 * @since 2.0.0
 * @category predicates
 */
export const isNoVote = vote => vote._tag === "NoVote";
/**
 * Check if a vote is a Yes vote.
 *
 * @since 2.0.0
 * @category predicates
 */
export const isYesVote = vote => vote._tag === "YesVote";
/**
 * Check if a vote is an Abstain vote.
 *
 * @since 2.0.0
 * @category predicates
 */
export const isAbstainVote = vote => vote._tag === "AbstainVote";
// ============================================================================
// Pattern Matching
// ============================================================================
/**
 * Pattern match on a Voter.
 *
 * @since 2.0.0
 * @category pattern matching
 */
export const matchVoter = patterns => voter => {
  switch (voter._tag) {
    case "ConstitutionalCommitteeVoter":
      return patterns.ConstitutionalCommitteeVoter(voter.credential);
    case "DRepVoter":
      return patterns.DRepVoter(voter.drep);
    case "StakePoolVoter":
      return patterns.StakePoolVoter(voter.poolKeyHash);
  }
};
/**
 * Pattern match on a Vote.
 *
 * @since 2.0.0
 * @category pattern matching
 */
export const matchVote = patterns => vote => {
  switch (vote._tag) {
    case "NoVote":
      return patterns.NoVote();
    case "YesVote":
      return patterns.YesVote();
    case "AbstainVote":
      return patterns.AbstainVote();
  }
};
// ============================================================================
// Arbitrary
// ============================================================================
/**
 * FastCheck arbitrary for VotingProcedures.
 *
 * @since 2.0.0
 * @category arbitrary
 */
export const arbitrary = /*#__PURE__*/FastCheck.array(FastCheck.tuple(
// Reuse existing voter arbitraries
FastCheck.oneof(Credential.arbitrary.map(credential => new ConstitutionalCommitteeVoter({
  credential
})),
// Only key/script DRep variants are valid Voter identifiers
FastCheck.oneof(KeyHash.arbitrary.map(keyHash => new DRep.KeyHashDRep({
  keyHash
})), ScriptHash.arbitrary.map(scriptHash => new DRep.ScriptHashDRep({
  scriptHash
}))).map(drep => new DRepVoter({
  drep
})), PoolKeyHash.arbitrary.map(poolKeyHash => new StakePoolVoter({
  poolKeyHash
}))), FastCheck.array(FastCheck.tuple(
// Create GovActionId instances using proper branded types
FastCheck.tuple(FastCheck.uint8Array({
  minLength: 32,
  maxLength: 32
}), FastCheck.bigInt({
  min: 0n,
  max: 65535n
})).map(([transactionId, govActionIndex]) => new GovernanceAction.GovActionId({
  transactionId: new TransactionHash.TransactionHash({
    hash: transactionId
  }),
  govActionIndex: Schema.decodeSync(Schema.typeSchema(TransactionIndex.TransactionIndex))(govActionIndex)
})), FastCheck.tuple(FastCheck.oneof(FastCheck.constant(new NoVote()), FastCheck.constant(new YesVote()), FastCheck.constant(new AbstainVote())), FastCheck.option(Anchor.arbitrary, {
  nil: null
})).map(([vote, anchor]) => new VotingProcedure({
  vote,
  anchor
})))).map(arr => new Map(arr)))).map(arr => new VotingProcedures({
  procedures: new Map(arr)
}));
// ============================================================================
// Root Functions
// ============================================================================
/**
 * Parse VotingProcedures from CBOR bytes.
 *
 * @since 2.0.0
 * @category parsing
 */
export const fromCBORBytes = (bytes, options = CBOR.CML_DEFAULT_OPTIONS) => Schema.decodeSync(FromCBORBytes(options))(bytes);
/**
 * Parse VotingProcedures from CBOR hex string.
 *
 * @since 2.0.0
 * @category parsing
 */
export const fromCBORHex = (hex, options = CBOR.CML_DEFAULT_OPTIONS) => Schema.decodeSync(FromCBORHex(options))(hex);
/**
 * Encode VotingProcedures to CBOR bytes.
 *
 * @since 2.0.0
 * @category encoding
 */
export const toCBORBytes = (data, options = CBOR.CML_DEFAULT_OPTIONS) => Schema.encodeSync(FromCBORBytes(options))(data);
/**
 * Encode VotingProcedures to CBOR hex string.
 *
 * @since 2.0.0
 * @category encoding
 */
export const toCBORHex = (data, options = CBOR.CML_DEFAULT_OPTIONS) => Schema.encodeSync(FromCBORHex(options))(data);
// ============================================================================
// Helper Functions
// ============================================================================
/**
 * Create VotingProcedures for a single vote.
 *
 * Convenience function for the common case of one voter voting on one proposal.
 *
 * @since 2.0.0
 * @category helpers
 */
export const singleVote = (voter, govActionId, procedure) => {
  return new VotingProcedures({
    procedures: new Map([[voter, new Map([[govActionId, procedure]])]])
  });
};
/**
 * Create VotingProcedures for one voter voting on multiple proposals.
 *
 * Convenience function for submitting multiple votes from a single voter.
 *
 * @since 2.0.0
 * @category helpers
 */
export const multiVote = (voter, votes) => {
  return new VotingProcedures({
    procedures: new Map([[voter, new Map(votes)]])
  });
};
//# sourceMappingURL=VotingProcedures.js.map