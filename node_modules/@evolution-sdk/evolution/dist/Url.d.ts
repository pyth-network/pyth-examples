import { Equal, Hash, Inspectable, Schema } from "effect";
declare const Url_base: Schema.TaggedClass<Url, "Url", {
    readonly _tag: Schema.tag<"Url">;
} & {
    href: Schema.refine<string, typeof Schema.String>;
}>;
/**
 * Schema for Url representing URLs as branded text.
 * url = text .size (0..128)
 *
 * @since 2.0.0
 * @category model
 */
export declare class Url extends Url_base {
    /**
     * Convert to JSON representation.
     *
     * @since 2.0.0
     * @category conversions
     */
    toJSON(): {
        _tag: string;
        href: string;
    };
    /**
     * Convert to string representation.
     *
     * @since 2.0.0
     * @category conversions
     */
    toString(): string;
    /**
     * Custom inspect for Node.js REPL.
     *
     * @since 2.0.0
     * @category conversions
     */
    [Inspectable.NodeInspectSymbol](): unknown;
    /**
     * Structural equality check.
     *
     * @since 2.0.0
     * @category equality
     */
    [Equal.symbol](that: unknown): boolean;
    /**
     * Hash code generation.
     *
     * @since 2.0.0
     * @category hashing
     */
    [Hash.symbol](): number;
}
export declare const FromBytes: Schema.transform<Schema.transform<Schema.transform<typeof Schema.Uint8ArrayFromSelf, typeof Schema.String>, Schema.refine<string, typeof Schema.String>>, typeof Url>;
export declare const FromHex: Schema.transform<Schema.Schema<Uint8Array<ArrayBufferLike>, string, never>, Schema.transform<Schema.transform<Schema.transform<typeof Schema.Uint8ArrayFromSelf, typeof Schema.String>, Schema.refine<string, typeof Schema.String>>, typeof Url>>;
/**
 * Check if the given value is a valid Url
 *
 * @since 2.0.0
 * @category predicates
 */
export declare const isUrl: (u: unknown, overrideOptions?: import("effect/SchemaAST").ParseOptions | number) => u is Url;
/**
 * FastCheck arbitrary for generating random Url instances.
 *
 * @since 2.0.0
 * @category arbitrary
 */
export declare const arbitrary: import("effect/FastCheck").Arbitrary<Url>;
/**
 * Parse Url from bytes.
 *
 * @since 2.0.0
 * @category parsing
 */
export declare const fromBytes: (bytes: Uint8Array) => Url;
/**
 * Parse Url from hex string.
 *
 * @since 2.0.0
 * @category parsing
 */
export declare const fromHex: (hex: string) => Url;
/**
 * Encode Url to bytes.
 *
 * @since 2.0.0
 * @category encoding
 */
export declare const toBytes: (url: Url) => Uint8Array<ArrayBufferLike>;
/**
 * Encode Url to hex string.
 *
 * @since 2.0.0
 * @category encoding
 */
export declare const toHex: (url: Url) => string;
export {};
//# sourceMappingURL=Url.d.ts.map