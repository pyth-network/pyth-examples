import { Data } from "effect";
/**
 * Error class for provider-related operations.
 *
 * @since 2.0.0
 * @category errors
 */
export class ProviderError extends /*#__PURE__*/Data.TaggedError("ProviderError") {}
/**
 * Preset retry configurations for common scenarios.
 *
 * @since 2.0.0
 * @category constants
 */
export const RetryPresets = {
  none: {
    maxRetries: 0,
    retryDelayMs: 0,
    backoffMultiplier: 1,
    maxRetryDelayMs: 0
  },
  fast: {
    maxRetries: 3,
    retryDelayMs: 500,
    backoffMultiplier: 1.5,
    maxRetryDelayMs: 5000
  },
  standard: {
    maxRetries: 3,
    retryDelayMs: 1000,
    backoffMultiplier: 2,
    maxRetryDelayMs: 10000
  },
  aggressive: {
    maxRetries: 5,
    retryDelayMs: 1000,
    backoffMultiplier: 2,
    maxRetryDelayMs: 30000
  }
};
//# sourceMappingURL=Client.js.map