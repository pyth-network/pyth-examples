import type { HttpClientResponse } from "@effect/platform";
import { HttpClient, HttpClientError } from "@effect/platform";
import { Effect, Schema } from "effect";
/**
 * Filter responses to only allow 2xx status codes, otherwise fail with ResponseError
 */
export declare const filterStatusOk: (self: HttpClientResponse.HttpClientResponse) => Effect.Effect<HttpClientResponse.HttpClientResponse, HttpClientError.ResponseError>;
/**
 * Performs a GET request and decodes the response using the provided schema
 */
export declare const get: <A, I, R>(url: string, schema: Schema.Schema<A, I, R>, headers?: Record<string, string>) => Effect.Effect<A, import("effect/ParseResult").ParseError | HttpClientError.HttpClientError, Exclude<R, HttpClient.HttpClient>>;
/**
 * Performs a POST request with JSON body and decodes the response using the provided schema
 */
export declare const postJson: <A, I, R>(url: string, body: unknown, schema: Schema.Schema<A, I, R>, headers?: Record<string, string>) => Effect.Effect<A, import("effect/ParseResult").ParseError | HttpClientError.HttpClientError | import("@effect/platform/HttpBody").HttpBodyError, Exclude<R, HttpClient.HttpClient>>;
/**
 * Performs a POST request with Uint8Array body and decodes the response using the provided schema
 */
export declare const postUint8Array: <A, I>(url: string, body: Uint8Array, schema: Schema.Schema<A, I>, headers?: Record<string, string>) => Effect.Effect<A, import("effect/ParseResult").ParseError | HttpClientError.HttpClientError, never>;
//# sourceMappingURL=HttpUtils.d.ts.map