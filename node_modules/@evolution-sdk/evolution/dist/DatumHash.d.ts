import { Equal, FastCheck, Hash, Inspectable, Schema } from "effect";
declare const DatumHash_base: Schema.TaggedClass<DatumHash, "DatumHash", {
    readonly _tag: Schema.tag<"DatumHash">;
} & {
    hash: Schema.filter<Schema.Schema<Uint8Array<ArrayBufferLike>, string, never>>;
}>;
/**
 * Schema for DatumHash representing a reference to datum data stored elsewhere via its hash.
 *
 * @since 2.0.0
 * @category schemas
 */
export declare class DatumHash extends DatumHash_base {
    /**
     * @since 2.0.0
     * @category json
     */
    toJSON(): {
        _tag: "DatumHash";
        hash: string;
    };
    /**
     * @since 2.0.0
     * @category string
     */
    toString(): string;
    /**
     * @since 2.0.0
     * @category inspect
     */
    [Inspectable.NodeInspectSymbol](): unknown;
    /**
     * @since 2.0.0
     * @category equality
     */
    [Equal.symbol](that: unknown): boolean;
    /**
     * @since 2.0.0
     * @category hash
     */
    [Hash.symbol](): number;
}
/**
 * Schema for transforming bytes to DatumHash.
 *
 * @since 2.0.0
 * @category schemas
 */
export declare const FromBytes: Schema.transform<Schema.SchemaClass<Uint8Array<ArrayBufferLike>, Uint8Array<ArrayBufferLike>, never>, Schema.SchemaClass<DatumHash, DatumHash, never>>;
/**
 * Schema for transforming hex string to DatumHash.
 *
 * @since 2.0.0
 * @category schemas
 */
export declare const FromHex: Schema.transform<Schema.filter<Schema.Schema<Uint8Array<ArrayBufferLike>, string, never>>, Schema.transform<Schema.SchemaClass<Uint8Array<ArrayBufferLike>, Uint8Array<ArrayBufferLike>, never>, Schema.SchemaClass<DatumHash, DatumHash, never>>>;
/**
 * FastCheck arbitrary for generating random DatumHash instances.
 *
 * @since 2.0.0
 * @category testing
 */
export declare const arbitrary: FastCheck.Arbitrary<DatumHash>;
/**
 * Type guard to check if a value is a DatumHash.
 *
 * @since 2.0.0
 * @category predicates
 */
export declare const isDatumHash: (u: unknown, overrideOptions?: import("effect/SchemaAST").ParseOptions | number) => u is DatumHash;
/**
 * Parse DatumHash from bytes.
 *
 * @since 2.0.0
 * @category parsing
 */
export declare const fromBytes: (i: Uint8Array<ArrayBufferLike>, overrideOptions?: import("effect/SchemaAST").ParseOptions) => DatumHash;
/**
 * Parse DatumHash from hex string.
 *
 * @since 2.0.0
 * @category parsing
 */
export declare const fromHex: (i: string, overrideOptions?: import("effect/SchemaAST").ParseOptions) => DatumHash;
/**
 * Encode DatumHash to bytes.
 *
 * @since 2.0.0
 * @category encoding
 */
export declare const toBytes: (a: DatumHash, overrideOptions?: import("effect/SchemaAST").ParseOptions) => Uint8Array<ArrayBufferLike>;
/**
 * Encode DatumHash to hex string.
 *
 * @since 2.0.0
 * @category encoding
 */
export declare const toHex: (a: DatumHash, overrideOptions?: import("effect/SchemaAST").ParseOptions) => string;
export {};
//# sourceMappingURL=DatumHash.d.ts.map