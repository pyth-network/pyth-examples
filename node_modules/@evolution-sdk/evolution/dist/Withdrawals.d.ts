import { Equal, FastCheck, Hash, Inspectable, Schema } from "effect";
import * as CBOR from "./CBOR.js";
import * as Coin from "./Coin.js";
import * as RewardAccount from "./RewardAccount.js";
declare const Withdrawals_base: Schema.TaggedClass<Withdrawals, "Withdrawals", {
    readonly _tag: Schema.tag<"Withdrawals">;
} & {
    withdrawals: Schema.Map$<Schema.transformOrFail<typeof Schema.String, Schema.SchemaClass<RewardAccount.RewardAccount, RewardAccount.RewardAccount, never>, never>, Schema.refine<bigint, typeof Schema.BigInt>>;
}>;
/**
 * Schema for Withdrawals representing a map of reward accounts to coin amounts.
 *
 * ```
 * withdrawals = {+ reward_account => coin}
 * ```
 *
 * @since 2.0.0
 * @category model
 */
export declare class Withdrawals extends Withdrawals_base {
    toJSON(): {
        _tag: "Withdrawals";
        withdrawals: Record<string, string>;
    };
    toString(): string;
    [Inspectable.NodeInspectSymbol](): unknown;
    [Equal.symbol](that: unknown): boolean;
    /**
     * Content-based hash for optimization of Equal.equals.
     *
     * @since 2.0.0
     * @category hashing
     */
    [Hash.symbol](): number;
}
/**
 * Check if the given value is a valid Withdrawals
 *
 * @since 2.0.0
 * @category predicates
 */
export declare const isWithdrawals: (u: unknown, overrideOptions?: import("effect/SchemaAST").ParseOptions | number) => u is Withdrawals;
export declare const CDDLSchema: Schema.MapFromSelf<typeof Schema.Uint8ArrayFromSelf, typeof Schema.BigIntFromSelf>;
/**
 * CDDL schema for Withdrawals.
 *
 * ```
 * withdrawals = {+ reward_account => coin}
 * ```
 *
 * @since 2.0.0
 * @category schemas
 */
export declare const FromCDDL: Schema.transformOrFail<Schema.MapFromSelf<typeof Schema.Uint8ArrayFromSelf, typeof Schema.BigIntFromSelf>, Schema.SchemaClass<Withdrawals, Withdrawals, never>, never>;
/**
 * CBOR bytes transformation schema for Withdrawals.
 *
 * @since 2.0.0
 * @category schemas
 */
export declare const FromCBORBytes: (options?: CBOR.CodecOptions) => Schema.transform<Schema.transformOrFail<typeof Schema.Uint8ArrayFromSelf, Schema.declare<CBOR.CBOR, CBOR.CBOR, readonly [], never>, never>, Schema.transformOrFail<Schema.MapFromSelf<typeof Schema.Uint8ArrayFromSelf, typeof Schema.BigIntFromSelf>, Schema.SchemaClass<Withdrawals, Withdrawals, never>, never>>;
/**
 * CBOR hex transformation schema for Withdrawals.
 *
 * @since 2.0.0
 * @category schemas
 */
export declare const FromCBORHex: (options?: CBOR.CodecOptions) => Schema.transform<Schema.Schema<Uint8Array<ArrayBufferLike>, string, never>, Schema.transform<Schema.transformOrFail<typeof Schema.Uint8ArrayFromSelf, Schema.declare<CBOR.CBOR, CBOR.CBOR, readonly [], never>, never>, Schema.transformOrFail<Schema.MapFromSelf<typeof Schema.Uint8ArrayFromSelf, typeof Schema.BigIntFromSelf>, Schema.SchemaClass<Withdrawals, Withdrawals, never>, never>>>;
/**
 * FastCheck arbitrary for Withdrawals instances.
 *
 * @since 2.0.0
 * @category testing
 */
export declare const arbitrary: FastCheck.Arbitrary<Withdrawals>;
/**
 * Create an empty Withdrawals instance.
 *
 * @since 2.0.0
 * @category constructors
 */
export declare const empty: () => Withdrawals;
/**
 * Create a Withdrawals instance with a single withdrawal.
 *
 * @since 2.0.0
 * @category constructors
 */
export declare const singleton: (rewardAccount: RewardAccount.RewardAccount, coin: Coin.Coin) => Withdrawals;
/**
 * Create a Withdrawals instance from an array of [RewardAccount, Coin] pairs.
 *
 * @since 2.0.0
 * @category constructors
 */
export declare const fromEntries: (entries: Array<[RewardAccount.RewardAccount, Coin.Coin]>) => Withdrawals;
/**
 * Add a withdrawal to existing Withdrawals.
 *
 * @since 2.0.0
 * @category transformation
 */
export declare const add: (withdrawals: Withdrawals, rewardAccount: RewardAccount.RewardAccount, coin: Coin.Coin) => Withdrawals;
/**
 * Remove a withdrawal from existing Withdrawals.
 *
 * @since 2.0.0
 * @category transformation
 */
export declare const remove: (withdrawals: Withdrawals, rewardAccount: RewardAccount.RewardAccount) => Withdrawals;
/**
 * Get the coin amount for a specific reward account.
 *
 * @since 2.0.0
 * @category transformation
 */
export declare const get: (withdrawals: Withdrawals, rewardAccount: RewardAccount.RewardAccount) => Coin.Coin | undefined;
/**
 * Check if Withdrawals contains a specific reward account.
 *
 * @since 2.0.0
 * @category predicates
 */
export declare const has: (withdrawals: Withdrawals, rewardAccount: RewardAccount.RewardAccount) => boolean;
/**
 * Check if Withdrawals is empty.
 *
 * @since 2.0.0
 * @category predicates
 */
export declare const isEmpty: (withdrawals: Withdrawals) => boolean;
/**
 * Get the size (number of withdrawals) in Withdrawals.
 *
 * @since 2.0.0
 * @category transformation
 */
export declare const size: (withdrawals: Withdrawals) => number;
/**
 * Get all entries as an array of [reward account, coin] pairs.
 *
 * @since 2.0.0
 * @category transformation
 */
export declare const entries: (withdrawals: Withdrawals) => Array<[RewardAccount.RewardAccount, Coin.Coin]>;
/**
 * Parse a Withdrawals from CBOR bytes.
 *
 * @since 2.0.0
 * @category parsing
 */
export declare const fromCBORBytes: (bytes: Uint8Array, options?: CBOR.CodecOptions) => Withdrawals;
/**
 * Parse a Withdrawals from CBOR hex string.
 *
 * @since 2.0.0
 * @category parsing
 */
export declare const fromCBORHex: (hex: string, options?: CBOR.CodecOptions) => Withdrawals;
/**
 * Convert a Withdrawals to CBOR bytes.
 *
 * @since 2.0.0
 * @category encoding
 */
export declare const toCBORBytes: (data: Withdrawals, options?: CBOR.CodecOptions) => Uint8Array<ArrayBufferLike>;
/**
 * Convert a Withdrawals to CBOR hex string.
 *
 * @since 2.0.0
 * @category encoding
 */
export declare const toCBORHex: (data: Withdrawals, options?: CBOR.CodecOptions) => string;
export {};
//# sourceMappingURL=Withdrawals.d.ts.map