import { Equal, FastCheck, Hash, Inspectable, Schema } from "effect";
import * as AssetName from "./AssetName.js";
import * as CBOR from "./CBOR.js";
import * as NonZeroInt64 from "./NonZeroInt64.js";
import * as PolicyId from "./PolicyId.js";
/**
 * Schema for inner asset map
 * ```
 * (asset_name => nonZeroInt64).
 * ```
 *
 * @since 2.0.0
 * @category schemas
 */
export declare const AssetMap: Schema.transform<Schema.Array$<Schema.Tuple2<typeof AssetName.AssetName, Schema.Union<[Schema.refine<bigint, typeof Schema.BigInt>, Schema.refine<bigint, typeof Schema.BigInt>]>>>, Schema.MapFromSelf<Schema.SchemaClass<AssetName.AssetName, AssetName.AssetName, never>, Schema.SchemaClass<bigint, bigint, never>>>;
export type AssetMap = typeof AssetMap.Type;
declare const Mint_base: Schema.Class<Mint, {
    map: Schema.Map$<typeof PolicyId.PolicyId, Schema.transform<Schema.Array$<Schema.Tuple2<typeof AssetName.AssetName, Schema.Union<[Schema.refine<bigint, typeof Schema.BigInt>, Schema.refine<bigint, typeof Schema.BigInt>]>>>, Schema.MapFromSelf<Schema.SchemaClass<AssetName.AssetName, AssetName.AssetName, never>, Schema.SchemaClass<bigint, bigint, never>>>>;
}, Schema.Struct.Encoded<{
    map: Schema.Map$<typeof PolicyId.PolicyId, Schema.transform<Schema.Array$<Schema.Tuple2<typeof AssetName.AssetName, Schema.Union<[Schema.refine<bigint, typeof Schema.BigInt>, Schema.refine<bigint, typeof Schema.BigInt>]>>>, Schema.MapFromSelf<Schema.SchemaClass<AssetName.AssetName, AssetName.AssetName, never>, Schema.SchemaClass<bigint, bigint, never>>>>;
}>, never, {
    readonly map: Map<PolicyId.PolicyId, Map<AssetName.AssetName, bigint>>;
}, {}, {}>;
/**
 * Schema for Mint representing token minting/burning operations.
 * ```
 * mint = multiasset<nonZeroInt64>
 *
 * The structure is: policy_id => { asset_name => nonZeroInt64 }
 * - Positive values represent minting
 * - Negative values represent burning
 * ```
 *
 * @since 2.0.0
 * @category model
 */
export declare class Mint extends Mint_base {
    /**
     * Convert to JSON representation.
     *
     * @since 2.0.0
     * @category conversions
     */
    toJSON(): {
        _tag: string;
        map: Record<string, Record<string, string>>;
    };
    /**
     * Convert to string representation.
     *
     * @since 2.0.0
     * @category conversions
     */
    toString(): string;
    /**
     * Custom inspect for Node.js REPL.
     *
     * @since 2.0.0
     * @category conversions
     */
    [Inspectable.NodeInspectSymbol](): unknown;
    /**
     * Structural equality check.
     *
     * @since 2.0.0
     * @category equality
     */
    [Equal.symbol](that: unknown): boolean;
    /**
     * @since 2.0.0
     * @category hashing
     */
    [Hash.symbol](): number;
}
/**
 * Check if a value is a valid Mint.
 *
 * @since 2.0.0
 * @category predicates
 */
export declare const is: (u: unknown, overrideOptions?: import("effect/SchemaAST").ParseOptions | number) => u is Mint;
/**
 * Create empty Mint.
 *
 * @since 2.0.0
 * @category constructors
 */
export declare const empty: () => Mint;
/**
 * Create Mint from a single policy and asset entry.
 *
 * @since 2.0.0
 * @category constructors
 */
export declare const singleton: (policyId: PolicyId.PolicyId, assetName: AssetName.AssetName, amount: NonZeroInt64.NonZeroInt64) => Mint;
/**
 * Create Mint from entries array.
 *
 * @since 2.0.0
 * @category constructors
 */
export declare const fromEntries: (entries: Array<[PolicyId.PolicyId, Array<[AssetName.AssetName, NonZeroInt64.NonZeroInt64]>]>) => Mint;
/**
 * Add or update an asset in the Mint.
 * Uses content-based equality (Equal.equals) to find matching PolicyId and AssetName
 * since JavaScript Maps use reference equality by default.
 *
 * @since 2.0.0
 * @category transformation
 */
export declare const insert: (mint: Mint, policyId: PolicyId.PolicyId, assetName: AssetName.AssetName, amount: NonZeroInt64.NonZeroInt64) => Mint;
/**
 * Remove a policy from the Mint.
 * Uses content-based equality (Equal.equals) to find matching PolicyId.
 *
 * @since 2.0.0
 * @category transformation
 */
export declare const removePolicy: (mint: Mint, policyId: PolicyId.PolicyId) => Mint;
/**
 * Remove an asset from the Mint.
 * Uses content-based equality (Equal.equals) to find matching PolicyId and AssetName.
 *
 * @since 2.0.0
 * @category transformation
 */
export declare const removeAsset: (mint: Mint, policyId: PolicyId.PolicyId, assetName: AssetName.AssetName) => Mint;
/**
 * Get the amount for a specific policy and asset.
 * Uses content-based equality (Equal.equals) to find matching PolicyId and AssetName.
 *
 * @since 2.0.0
 * @category transformation
 */
export declare const get: (mint: Mint, policyId: PolicyId.PolicyId, assetName: AssetName.AssetName) => bigint | undefined;
/**
 * Check if Mint contains a specific policy and asset.
 *
 * @since 2.0.0
 * @category predicates
 */
export declare const has: (mint: Mint, policyId: PolicyId.PolicyId, assetName: AssetName.AssetName) => boolean;
/**
 * Get an asset amount by policy ID hex and asset name hex strings.
 * Convenience function for tests and lookups using hex strings.
 *
 * @since 2.0.0
 * @category lookup
 */
export declare const getByHex: (mint: Mint, policyIdHex: string, assetNameHex: string) => NonZeroInt64.NonZeroInt64 | undefined;
/**
 * Get the asset map for a specific policy by hex string.
 * Uses content-based equality (Equal.equals) to find matching PolicyId.
 *
 * @since 2.0.0
 * @category lookup
 */
export declare const getAssetsByPolicyHex: (mint: Mint, policyIdHex: string) => Map<AssetName.AssetName, NonZeroInt64.NonZeroInt64> | undefined;
/**
 * Check if Mint is empty.
 *
 * @since 2.0.0
 * @category predicates
 */
export declare const isEmpty: (mint: Mint) => boolean;
/**
 * Get the number of policies in the Mint.
 *
 * @since 2.0.0
 * @category transformation
 */
export declare const policyCount: (mint: Mint) => number;
export declare const CDDLSchema: Schema.MapFromSelf<typeof Schema.Uint8ArrayFromSelf, Schema.MapFromSelf<typeof Schema.Uint8ArrayFromSelf, typeof Schema.BigIntFromSelf>>;
/**
 * CDDL schema for Mint as map structure.
 * ```
 * mint = {* policy_id => {* asset_name => nonZeroInt64}}
 * ```
 *
 * Where:
 * - policy_id: 28-byte Uint8Array (from CBOR byte string)
 * - asset_name: variable-length Uint8Array (from CBOR byte string, can be empty)
 * - nonZeroInt64: signed 64-bit integer (positive = mint, negative = burn, cannot be zero)
 *
 * @since 2.0.0
 * @category schemas
 */
export declare const FromCDDL: Schema.transformOrFail<Schema.SchemaClass<ReadonlyMap<Uint8Array<ArrayBufferLike>, ReadonlyMap<Uint8Array<ArrayBufferLike>, bigint>>, ReadonlyMap<Uint8Array<ArrayBufferLike>, ReadonlyMap<Uint8Array<ArrayBufferLike>, bigint>>, never>, Schema.SchemaClass<Mint, Mint, never>, never>;
/**
 * CBOR bytes transformation schema for Mint.
 * Transforms between CBOR bytes and Mint using CBOR encoding.
 *
 * @since 2.0.0
 * @category schemas
 */
export declare const FromCBORBytes: (options?: CBOR.CodecOptions) => Schema.transform<Schema.transformOrFail<typeof Schema.Uint8ArrayFromSelf, Schema.declare<CBOR.CBOR, CBOR.CBOR, readonly [], never>, never>, Schema.transformOrFail<Schema.SchemaClass<ReadonlyMap<Uint8Array<ArrayBufferLike>, ReadonlyMap<Uint8Array<ArrayBufferLike>, bigint>>, ReadonlyMap<Uint8Array<ArrayBufferLike>, ReadonlyMap<Uint8Array<ArrayBufferLike>, bigint>>, never>, Schema.SchemaClass<Mint, Mint, never>, never>>;
/**
 * CBOR hex transformation schema for Mint.
 * Transforms between CBOR hex string and Mint using CBOR encoding.
 *
 * @since 2.0.0
 * @category schemas
 */
export declare const FromCBORHex: (options?: CBOR.CodecOptions) => Schema.transform<Schema.Schema<Uint8Array<ArrayBufferLike>, string, never>, Schema.transform<Schema.transformOrFail<typeof Schema.Uint8ArrayFromSelf, Schema.declare<CBOR.CBOR, CBOR.CBOR, readonly [], never>, never>, Schema.transformOrFail<Schema.SchemaClass<ReadonlyMap<Uint8Array<ArrayBufferLike>, ReadonlyMap<Uint8Array<ArrayBufferLike>, bigint>>, ReadonlyMap<Uint8Array<ArrayBufferLike>, ReadonlyMap<Uint8Array<ArrayBufferLike>, bigint>>, never>, Schema.SchemaClass<Mint, Mint, never>, never>>>;
/**
 * FastCheck arbitrary for generating random Mint instances.
 *
 * @since 2.0.0
 * @category arbitrary
 */
export declare const arbitrary: FastCheck.Arbitrary<Mint>;
/**
 * Parse Mint from CBOR bytes.
 *
 * @since 2.0.0
 * @category parsing
 */
export declare const fromCBORBytes: (bytes: Uint8Array, options?: CBOR.CodecOptions) => Mint;
/**
 * Parse Mint from CBOR hex string.
 *
 * @since 2.0.0
 * @category parsing
 */
export declare const fromCBORHex: (hex: string, options?: CBOR.CodecOptions) => Mint;
/**
 * Encode Mint to CBOR bytes.
 *
 * @since 2.0.0
 * @category encoding
 */
export declare const toCBORBytes: (mint: Mint, options?: CBOR.CodecOptions) => Uint8Array;
/**
 * Encode Mint to CBOR hex string.
 *
 * @since 2.0.0
 * @category encoding
 */
export declare const toCBORHex: (mint: Mint, options?: CBOR.CodecOptions) => string;
export {};
//# sourceMappingURL=Mint.d.ts.map