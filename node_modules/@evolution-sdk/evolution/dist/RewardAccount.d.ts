import { Equal, FastCheck, Hash, Inspectable, Schema } from "effect";
import * as KeyHash from "./KeyHash.js";
import * as ScriptHash from "./ScriptHash.js";
declare const RewardAccount_base: Schema.TaggedClass<RewardAccount, "RewardAccount", {
    readonly _tag: Schema.tag<"RewardAccount">;
} & {
    networkId: Schema.refine<number, typeof Schema.NonNegative>;
    stakeCredential: Schema.Union<[typeof KeyHash.KeyHash, typeof ScriptHash.ScriptHash]>;
}>;
/**
 * Reward/stake address with only staking credential
 *
 * @since 2.0.0
 * @category schemas
 */
export declare class RewardAccount extends RewardAccount_base {
    /**
     * @since 2.0.0
     * @category json
     */
    toJSON(): {
        _tag: "RewardAccount";
        networkId: number;
        stakeCredential: KeyHash.KeyHash | ScriptHash.ScriptHash;
    };
    /**
     * @since 2.0.0
     * @category string
     */
    toString(): string;
    /**
     * @since 2.0.0
     * @category inspect
     */
    [Inspectable.NodeInspectSymbol](): unknown;
    /**
     * @since 2.0.0
     * @category equality
     */
    [Equal.symbol](that: unknown): boolean;
    /**
     * @since 2.0.0
     * @category hash
     */
    [Hash.symbol](): number;
}
export declare const FromBytes: Schema.transformOrFail<Schema.SchemaClass<Uint8Array<ArrayBufferLike>, Uint8Array<ArrayBufferLike>, never>, Schema.SchemaClass<RewardAccount, RewardAccount, never>, never>;
export declare const FromHex: Schema.transform<Schema.Schema<Uint8Array<ArrayBufferLike>, string, never>, Schema.transformOrFail<Schema.SchemaClass<Uint8Array<ArrayBufferLike>, Uint8Array<ArrayBufferLike>, never>, Schema.SchemaClass<RewardAccount, RewardAccount, never>, never>>;
export declare const FromBech32: Schema.transformOrFail<typeof Schema.String, Schema.SchemaClass<RewardAccount, RewardAccount, never>, never>;
/**
 * FastCheck arbitrary for RewardAccount instances.
 *
 * @since 2.0.0
 * @category arbitrary
 */
export declare const arbitrary: FastCheck.Arbitrary<RewardAccount>;
/**
 * Parse a RewardAccount from bytes.
 *
 * @since 2.0.0
 * @category parsing
 */
export declare const fromBytes: (bytes: Uint8Array) => RewardAccount;
/**
 * Parse a RewardAccount from hex string.
 *
 * @since 2.0.0
 * @category parsing
 */
export declare const fromHex: (hex: string) => RewardAccount;
/**
 * Parse a RewardAccount from Bech32 string.
 *
 * @since 2.0.0
 * @category parsing
 */
export declare const fromBech32: (str: string) => RewardAccount;
/**
 * Convert a RewardAccount to bytes.
 *
 * @since 2.0.0
 * @category encoding
 */
export declare const toBytes: (data: RewardAccount) => Uint8Array<ArrayBufferLike>;
/**
 * Convert a RewardAccount to hex string.
 *
 * @since 2.0.0
 * @category encoding
 */
export declare const toHex: (data: RewardAccount) => string;
/**
 * Convert a RewardAccount to Bech32 string.
 *
 * @since 2.0.0
 * @category encoding
 */
export declare const toBech32: (data: RewardAccount) => string;
export {};
//# sourceMappingURL=RewardAccount.d.ts.map