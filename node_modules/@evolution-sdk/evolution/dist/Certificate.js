import { Either as E, Equal, FastCheck, Hash, Inspectable, ParseResult, Schema } from "effect";
import * as Anchor from "./Anchor.js";
import * as CBOR from "./CBOR.js";
import * as Coin from "./Coin.js";
import * as Credential from "./Credential.js";
import * as DRep from "./DRep.js";
import * as EpochNo from "./EpochNo.js";
import * as PoolKeyHash from "./PoolKeyHash.js";
import * as PoolMetadata from "./PoolMetadata.js";
import * as PoolParams from "./PoolParams.js";
import * as Relay from "./Relay.js";
import * as UnitInterval from "./UnitInterval.js";
export class StakeRegistration extends /*#__PURE__*/Schema.TaggedClass("StakeRegistration")("StakeRegistration", {
  stakeCredential: Credential.Credential
}) {
  toJSON() {
    return {
      _tag: "StakeRegistration",
      stakeCredential: this.stakeCredential.toJSON()
    };
  }
  toString() {
    return Inspectable.format(this.toJSON());
  }
  [Inspectable.NodeInspectSymbol]() {
    return this.toJSON();
  }
  [Equal.symbol](that) {
    return that instanceof StakeRegistration && Equal.equals(this.stakeCredential, that.stakeCredential);
  }
  [Hash.symbol]() {
    return Hash.cached(this, Hash.combine(Hash.hash("StakeRegistration"))(Hash.hash(this.stakeCredential)));
  }
}
export class StakeDeregistration extends /*#__PURE__*/Schema.TaggedClass("StakeDeregistration")("StakeDeregistration", {
  stakeCredential: Credential.Credential
}) {
  toJSON() {
    return {
      _tag: "StakeDeregistration",
      stakeCredential: this.stakeCredential.toJSON()
    };
  }
  toString() {
    return Inspectable.format(this.toJSON());
  }
  [Inspectable.NodeInspectSymbol]() {
    return this.toJSON();
  }
  [Equal.symbol](that) {
    return that instanceof StakeDeregistration && Equal.equals(this.stakeCredential, that.stakeCredential);
  }
  [Hash.symbol]() {
    return Hash.cached(this, Hash.combine(Hash.hash("StakeDeregistration"))(Hash.hash(this.stakeCredential)));
  }
}
export class StakeDelegation extends /*#__PURE__*/Schema.TaggedClass("StakeDelegation")("StakeDelegation", {
  stakeCredential: Credential.Credential,
  poolKeyHash: PoolKeyHash.PoolKeyHash
}) {
  toJSON() {
    return {
      _tag: "StakeDelegation",
      stakeCredential: this.stakeCredential.toJSON(),
      poolKeyHash: this.poolKeyHash.toJSON()
    };
  }
  toString() {
    return Inspectable.format(this.toJSON());
  }
  [Inspectable.NodeInspectSymbol]() {
    return this.toJSON();
  }
  [Equal.symbol](that) {
    return that instanceof StakeDelegation && Equal.equals(this.stakeCredential, that.stakeCredential) && Equal.equals(this.poolKeyHash, that.poolKeyHash);
  }
  [Hash.symbol]() {
    return Hash.cached(this, Hash.combine(Hash.hash("StakeDelegation"))(Hash.combine(Hash.hash(this.stakeCredential))(Hash.hash(this.poolKeyHash))));
  }
}
export class PoolRegistration extends /*#__PURE__*/Schema.TaggedClass("PoolRegistration")("PoolRegistration", {
  poolParams: PoolParams.PoolParams
}) {
  toJSON() {
    return {
      _tag: "PoolRegistration",
      poolParams: this.poolParams.toJSON()
    };
  }
  toString() {
    return Inspectable.format(this.toJSON());
  }
  [Inspectable.NodeInspectSymbol]() {
    return this.toJSON();
  }
  [Equal.symbol](that) {
    return that instanceof PoolRegistration && Equal.equals(this.poolParams, that.poolParams);
  }
  [Hash.symbol]() {
    return Hash.cached(this, Hash.combine(Hash.hash("PoolRegistration"))(Hash.hash(this.poolParams)));
  }
}
export class PoolRetirement extends /*#__PURE__*/Schema.TaggedClass("PoolRetirement")("PoolRetirement", {
  poolKeyHash: PoolKeyHash.PoolKeyHash,
  epoch: EpochNo.EpochNoSchema
}) {
  toJSON() {
    return {
      _tag: "PoolRetirement",
      poolKeyHash: this.poolKeyHash.toJSON(),
      epoch: this.epoch.toString()
    };
  }
  toString() {
    return Inspectable.format(this.toJSON());
  }
  [Inspectable.NodeInspectSymbol]() {
    return this.toJSON();
  }
  [Equal.symbol](that) {
    return that instanceof PoolRetirement && Equal.equals(this.poolKeyHash, that.poolKeyHash) && Equal.equals(this.epoch, that.epoch);
  }
  [Hash.symbol]() {
    return Hash.cached(this, Hash.combine(Hash.hash("PoolRetirement"))(Hash.combine(Hash.hash(this.poolKeyHash))(Hash.hash(this.epoch))));
  }
}
export class RegCert extends /*#__PURE__*/Schema.TaggedClass("RegCert")("RegCert", {
  stakeCredential: Credential.Credential,
  coin: Coin.Coin
}) {
  toJSON() {
    return {
      _tag: "RegCert",
      stakeCredential: this.stakeCredential.toJSON(),
      coin: this.coin.toString()
    };
  }
  toString() {
    return Inspectable.format(this.toJSON());
  }
  [Inspectable.NodeInspectSymbol]() {
    return this.toJSON();
  }
  [Equal.symbol](that) {
    return that instanceof RegCert && Equal.equals(this.stakeCredential, that.stakeCredential) && Equal.equals(this.coin, that.coin);
  }
  [Hash.symbol]() {
    return Hash.cached(this, Hash.combine(Hash.hash("RegCert"))(Hash.combine(Hash.hash(this.stakeCredential))(Hash.hash(this.coin))));
  }
}
export class UnregCert extends /*#__PURE__*/Schema.TaggedClass("UnregCert")("UnregCert", {
  stakeCredential: Credential.Credential,
  coin: Coin.Coin
}) {
  toJSON() {
    return {
      _tag: "UnregCert",
      stakeCredential: this.stakeCredential.toJSON(),
      coin: this.coin.toString()
    };
  }
  toString() {
    return Inspectable.format(this.toJSON());
  }
  [Inspectable.NodeInspectSymbol]() {
    return this.toJSON();
  }
  [Equal.symbol](that) {
    return that instanceof UnregCert && Equal.equals(this.stakeCredential, that.stakeCredential) && Equal.equals(this.coin, that.coin);
  }
  [Hash.symbol]() {
    return Hash.cached(this, Hash.combine(Hash.hash("UnregCert"))(Hash.combine(Hash.hash(this.stakeCredential))(Hash.hash(this.coin))));
  }
}
export class VoteDelegCert extends /*#__PURE__*/Schema.TaggedClass("VoteDelegCert")("VoteDelegCert", {
  stakeCredential: Credential.Credential,
  drep: DRep.DRep
}) {
  toJSON() {
    return {
      _tag: "VoteDelegCert",
      stakeCredential: this.stakeCredential.toJSON(),
      drep: this.drep
    };
  }
  toString() {
    return Inspectable.format(this.toJSON());
  }
  [Inspectable.NodeInspectSymbol]() {
    return this.toJSON();
  }
  [Equal.symbol](that) {
    return that instanceof VoteDelegCert && Equal.equals(this.stakeCredential, that.stakeCredential) && Equal.equals(this.drep, that.drep);
  }
  [Hash.symbol]() {
    return Hash.cached(this, Hash.combine(Hash.hash("VoteDelegCert"))(Hash.combine(Hash.hash(this.stakeCredential))(Hash.hash(this.drep))));
  }
}
export class StakeVoteDelegCert extends /*#__PURE__*/Schema.TaggedClass("StakeVoteDelegCert")("StakeVoteDelegCert", {
  stakeCredential: Credential.Credential,
  poolKeyHash: PoolKeyHash.PoolKeyHash,
  drep: DRep.DRep
}) {
  toJSON() {
    return {
      _tag: "StakeVoteDelegCert",
      stakeCredential: this.stakeCredential.toJSON(),
      poolKeyHash: this.poolKeyHash.toJSON(),
      drep: this.drep
    };
  }
  toString() {
    return Inspectable.format(this.toJSON());
  }
  [Inspectable.NodeInspectSymbol]() {
    return this.toJSON();
  }
  [Equal.symbol](that) {
    return that instanceof StakeVoteDelegCert && Equal.equals(this.stakeCredential, that.stakeCredential) && Equal.equals(this.poolKeyHash, that.poolKeyHash) && Equal.equals(this.drep, that.drep);
  }
  [Hash.symbol]() {
    return Hash.cached(this, Hash.combine(Hash.hash("StakeVoteDelegCert"))(Hash.combine(Hash.hash(this.stakeCredential))(Hash.combine(Hash.hash(this.poolKeyHash))(Hash.hash(this.drep)))));
  }
}
export class StakeRegDelegCert extends /*#__PURE__*/Schema.TaggedClass("StakeRegDelegCert")("StakeRegDelegCert", {
  stakeCredential: Credential.Credential,
  poolKeyHash: PoolKeyHash.PoolKeyHash,
  coin: Coin.Coin
}) {
  toJSON() {
    return {
      _tag: "StakeRegDelegCert",
      stakeCredential: this.stakeCredential.toJSON(),
      poolKeyHash: this.poolKeyHash.toJSON(),
      coin: this.coin.toString()
    };
  }
  toString() {
    return Inspectable.format(this.toJSON());
  }
  [Inspectable.NodeInspectSymbol]() {
    return this.toJSON();
  }
  [Equal.symbol](that) {
    return that instanceof StakeRegDelegCert && Equal.equals(this.stakeCredential, that.stakeCredential) && Equal.equals(this.poolKeyHash, that.poolKeyHash) && Equal.equals(this.coin, that.coin);
  }
  [Hash.symbol]() {
    return Hash.cached(this, Hash.combine(Hash.hash("StakeRegDelegCert"))(Hash.combine(Hash.hash(this.stakeCredential))(Hash.combine(Hash.hash(this.poolKeyHash))(Hash.hash(this.coin)))));
  }
}
export class VoteRegDelegCert extends /*#__PURE__*/Schema.TaggedClass("VoteRegDelegCert")("VoteRegDelegCert", {
  stakeCredential: Credential.Credential,
  drep: DRep.DRep,
  coin: Coin.Coin
}) {
  toJSON() {
    return {
      _tag: "VoteRegDelegCert",
      stakeCredential: this.stakeCredential.toJSON(),
      drep: this.drep,
      coin: this.coin.toString()
    };
  }
  toString() {
    return Inspectable.format(this.toJSON());
  }
  [Inspectable.NodeInspectSymbol]() {
    return this.toJSON();
  }
  [Equal.symbol](that) {
    return that instanceof VoteRegDelegCert && Equal.equals(this.stakeCredential, that.stakeCredential) && Equal.equals(this.drep, that.drep) && Equal.equals(this.coin, that.coin);
  }
  [Hash.symbol]() {
    return Hash.cached(this, Hash.combine(Hash.hash("VoteRegDelegCert"))(Hash.combine(Hash.hash(this.stakeCredential))(Hash.combine(Hash.hash(this.drep))(Hash.hash(this.coin)))));
  }
}
export class StakeVoteRegDelegCert extends /*#__PURE__*/Schema.TaggedClass("StakeVoteRegDelegCert")("StakeVoteRegDelegCert", {
  stakeCredential: Credential.Credential,
  poolKeyHash: PoolKeyHash.PoolKeyHash,
  drep: DRep.DRep,
  coin: Coin.Coin
}) {
  toJSON() {
    return {
      _tag: "StakeVoteRegDelegCert",
      stakeCredential: this.stakeCredential.toJSON(),
      poolKeyHash: this.poolKeyHash.toJSON(),
      drep: this.drep,
      coin: this.coin.toString()
    };
  }
  toString() {
    return Inspectable.format(this.toJSON());
  }
  [Inspectable.NodeInspectSymbol]() {
    return this.toJSON();
  }
  [Equal.symbol](that) {
    return that instanceof StakeVoteRegDelegCert && Equal.equals(this.stakeCredential, that.stakeCredential) && Equal.equals(this.poolKeyHash, that.poolKeyHash) && Equal.equals(this.drep, that.drep) && Equal.equals(this.coin, that.coin);
  }
  [Hash.symbol]() {
    return Hash.cached(this, Hash.combine(Hash.hash("StakeVoteRegDelegCert"))(Hash.combine(Hash.hash(this.stakeCredential))(Hash.combine(Hash.hash(this.poolKeyHash))(Hash.combine(Hash.hash(this.drep))(Hash.hash(this.coin))))));
  }
}
export class AuthCommitteeHotCert extends /*#__PURE__*/Schema.TaggedClass("AuthCommitteeHotCert")("AuthCommitteeHotCert", {
  committeeColdCredential: Credential.Credential,
  committeeHotCredential: Credential.Credential
}) {
  toJSON() {
    return {
      _tag: "AuthCommitteeHotCert",
      committeeColdCredential: this.committeeColdCredential.toJSON(),
      committeeHotCredential: this.committeeHotCredential.toJSON()
    };
  }
  toString() {
    return Inspectable.format(this.toJSON());
  }
  [Inspectable.NodeInspectSymbol]() {
    return this.toJSON();
  }
  [Equal.symbol](that) {
    return that instanceof AuthCommitteeHotCert && Equal.equals(this.committeeColdCredential, that.committeeColdCredential) && Equal.equals(this.committeeHotCredential, that.committeeHotCredential);
  }
  [Hash.symbol]() {
    return Hash.cached(this, Hash.combine(Hash.hash("AuthCommitteeHotCert"))(Hash.combine(Hash.hash(this.committeeColdCredential))(Hash.hash(this.committeeHotCredential))));
  }
}
export class ResignCommitteeColdCert extends /*#__PURE__*/Schema.TaggedClass("ResignCommitteeColdCert")("ResignCommitteeColdCert", {
  committeeColdCredential: Credential.Credential,
  anchor: /*#__PURE__*/Schema.NullishOr(Anchor.Anchor)
}) {
  toJSON() {
    return {
      _tag: "ResignCommitteeColdCert",
      committeeColdCredential: this.committeeColdCredential.toJSON(),
      anchor: this.anchor
    };
  }
  toString() {
    return Inspectable.format(this.toJSON());
  }
  [Inspectable.NodeInspectSymbol]() {
    return this.toJSON();
  }
  [Equal.symbol](that) {
    return that instanceof ResignCommitteeColdCert && Equal.equals(this.committeeColdCredential, that.committeeColdCredential) && Equal.equals(this.anchor, that.anchor);
  }
  [Hash.symbol]() {
    return Hash.cached(this, Hash.combine(Hash.hash("ResignCommitteeColdCert"))(Hash.combine(Hash.hash(this.committeeColdCredential))(Hash.hash(this.anchor))));
  }
}
export class RegDrepCert extends /*#__PURE__*/Schema.TaggedClass("RegDrepCert")("RegDrepCert", {
  drepCredential: Credential.Credential,
  coin: Coin.Coin,
  anchor: /*#__PURE__*/Schema.NullishOr(Anchor.Anchor)
}) {
  toJSON() {
    return {
      _tag: "RegDrepCert",
      drepCredential: this.drepCredential.toJSON(),
      coin: this.coin.toString(),
      anchor: this.anchor
    };
  }
  toString() {
    return Inspectable.format(this.toJSON());
  }
  [Inspectable.NodeInspectSymbol]() {
    return this.toJSON();
  }
  [Equal.symbol](that) {
    return that instanceof RegDrepCert && Equal.equals(this.drepCredential, that.drepCredential) && Equal.equals(this.coin, that.coin) && Equal.equals(this.anchor, that.anchor);
  }
  [Hash.symbol]() {
    return Hash.cached(this, Hash.combine(Hash.hash("RegDrepCert"))(Hash.combine(Hash.hash(this.drepCredential))(Hash.combine(Hash.hash(this.coin))(Hash.hash(this.anchor)))));
  }
}
export class UnregDrepCert extends /*#__PURE__*/Schema.TaggedClass("UnregDrepCert")("UnregDrepCert", {
  drepCredential: Credential.Credential,
  coin: Coin.Coin
}) {
  toJSON() {
    return {
      _tag: "UnregDrepCert",
      drepCredential: this.drepCredential.toJSON(),
      coin: this.coin.toString()
    };
  }
  toString() {
    return Inspectable.format(this.toJSON());
  }
  [Inspectable.NodeInspectSymbol]() {
    return this.toJSON();
  }
  [Equal.symbol](that) {
    return that instanceof UnregDrepCert && Equal.equals(this.drepCredential, that.drepCredential) && Equal.equals(this.coin, that.coin);
  }
  [Hash.symbol]() {
    return Hash.cached(this, Hash.combine(Hash.hash("UnregDrepCert"))(Hash.combine(Hash.hash(this.drepCredential))(Hash.hash(this.coin))));
  }
}
export class UpdateDrepCert extends /*#__PURE__*/Schema.TaggedClass("UpdateDrepCert")("UpdateDrepCert", {
  drepCredential: Credential.Credential,
  anchor: /*#__PURE__*/Schema.NullishOr(Anchor.Anchor)
}) {
  toJSON() {
    return {
      _tag: "UpdateDrepCert",
      drepCredential: this.drepCredential.toJSON(),
      anchor: this.anchor
    };
  }
  toString() {
    return Inspectable.format(this.toJSON());
  }
  [Inspectable.NodeInspectSymbol]() {
    return this.toJSON();
  }
  [Equal.symbol](that) {
    return that instanceof UpdateDrepCert && Equal.equals(this.drepCredential, that.drepCredential) && Equal.equals(this.anchor, that.anchor);
  }
  [Hash.symbol]() {
    return Hash.cached(this, Hash.combine(Hash.hash("UpdateDrepCert"))(Hash.combine(Hash.hash(this.drepCredential))(Hash.hash(this.anchor))));
  }
}
/**
 * Certificate union schema based on Conway CDDL specification
 *
 * CDDL: certificate =
 *   [
 *   stake_registration
 *   // stake_deregistration
 *   // stake_delegation
 *   // pool_registration
 *   // pool_retirement
 *   // reg_cert
 *   // unreg_cert
 *   // vote_deleg_cert
 *   // stake_vote_deleg_cert
 *   // stake_reg_deleg_cert
 *   // vote_reg_deleg_cert
 *   // stake_vote_reg_deleg_cert
 *   // auth_committee_hot_cert
 *   // resign_committee_cold_cert
 *   // reg_drep_cert
 *   // unreg_drep_cert
 *   // update_drep_cert
 *   ]
 *
 * stake_registration = (0, stake_credential)
 * stake_deregistration = (1, stake_credential)
 * stake_delegation = (2, stake_credential, pool_keyhash)
 * pool_registration = (3, pool_params)
 * pool_retirement = (4, pool_keyhash, epoch_no)
 * reg_cert = (7, stake_credential, coin)
 * unreg_cert = (8, stake_credential, coin)
 * vote_deleg_cert = (9, stake_credential, drep)
 * stake_vote_deleg_cert = (10, stake_credential, pool_keyhash, drep)
 * stake_reg_deleg_cert = (11, stake_credential, pool_keyhash, coin)
 * vote_reg_deleg_cert = (12, stake_credential, drep, coin)
 * stake_vote_reg_deleg_cert = (13, stake_credential, pool_keyhash, drep, coin)
 * auth_committee_hot_cert = (14, committee_cold_credential, committee_hot_credential)
 * resign_committee_cold_cert = (15, committee_cold_credential, anchor/ nil)
 * reg_drep_cert = (16, drep_credential, coin, anchor/ nil)
 * unreg_drep_cert = (17, drep_credential, coin)
 * update_drep_cert = (18, drep_credential, anchor/ nil)
 *
 * @since 2.0.0
 * @category schemas
 */
export const Certificate = /*#__PURE__*/Schema.Union(
// 0: stake_registration = (0, stake_credential)
StakeRegistration,
// 1: stake_deregistration = (1, stake_credential)
StakeDeregistration,
// 2: stake_delegation = (2, stake_credential, pool_keyhash)
StakeDelegation,
// 3: pool_registration = (3, pool_params)
PoolRegistration,
// 4: pool_retirement = (4, pool_keyhash, epoch_no)
PoolRetirement,
// 7: reg_cert = (7, stake_credential, coin)
RegCert,
// 8: unreg_cert = (8, stake_credential, coin)
UnregCert,
// 9: vote_deleg_cert = (9, stake_credential, drep)
VoteDelegCert,
// 10: stake_vote_deleg_cert = (10, stake_credential, pool_keyhash, drep)
StakeVoteDelegCert,
// 11: stake_reg_deleg_cert = (11, stake_credential, pool_keyhash, coin)
StakeRegDelegCert,
// 12: vote_reg_deleg_cert = (12, stake_credential, drep, coin)
VoteRegDelegCert,
// 13: stake_vote_reg_deleg_cert = (13, stake_credential, pool_keyhash, drep, coin)
StakeVoteRegDelegCert,
// 14: auth_committee_hot_cert = (14, committee_cold_credential, committee_hot_credential)
AuthCommitteeHotCert,
// 15: resign_committee_cold_cert = (15, committee_cold_credential, anchor/ nil)
ResignCommitteeColdCert,
// 16: reg_drep_cert = (16, drep_credential, coin, anchor/ nil)
RegDrepCert,
// 17: unreg_drep_cert = (17, drep_credential, coin)
UnregDrepCert,
// 18: update_drep_cert = (18, drep_credential, anchor/ nil)
UpdateDrepCert);
export const CDDLSchema = /*#__PURE__*/Schema.Union(
/*#__PURE__*/
// 0: stake_registration = (0, stake_credential)
Schema.Tuple(/*#__PURE__*/Schema.Literal(0n), Credential.CDDLSchema),
/*#__PURE__*/
// 1: stake_deregistration = (1, stake_credential)
Schema.Tuple(/*#__PURE__*/Schema.Literal(1n), Credential.CDDLSchema),
/*#__PURE__*/
// 2: stake_delegation = (2, stake_credential, pool_keyhash)
Schema.Tuple(/*#__PURE__*/Schema.Literal(2n), Credential.CDDLSchema, CBOR.ByteArray),
/*#__PURE__*/
// 3: pool_registration = (3, pool_params)
Schema.Tuple(/*#__PURE__*/Schema.Literal(3n),
// Flattened PoolParams.CDDLSchema
CBOR.ByteArray,
// operator (pool_keyhash as bytes)
CBOR.ByteArray,
// vrf_keyhash (as bytes)
CBOR.Integer,
// pledge (coin)
CBOR.Integer,
// cost (coin)
UnitInterval.CDDLSchema,
// margin
CBOR.ByteArray,
/*#__PURE__*/
// reward_account (bytes)
Schema.Array(CBOR.ByteArray),
/*#__PURE__*/
// pool_owners (array of addr_keyhash bytes)
Schema.Array(/*#__PURE__*/Schema.encodedSchema(Relay.FromCDDL)),
/*#__PURE__*/
// relays
Schema.NullOr(/*#__PURE__*/Schema.encodedSchema(PoolMetadata.FromCDDL)) // pool_metadata
),
/*#__PURE__*/
// 4: pool_retirement = (4, pool_keyhash, epoch_no)
Schema.Tuple(/*#__PURE__*/Schema.Literal(4n), CBOR.ByteArray, CBOR.Integer),
/*#__PURE__*/
// 7: reg_cert = (7, stake_credential , coin)
Schema.Tuple(/*#__PURE__*/Schema.Literal(7n), Credential.CDDLSchema, CBOR.Integer),
/*#__PURE__*/
// 8: unreg_cert = (8, stake_credential, coin)
Schema.Tuple(/*#__PURE__*/Schema.Literal(8n), Credential.CDDLSchema, CBOR.Integer),
/*#__PURE__*/
// 9: vote_deleg_cert = (9, stake_credential, drep)
Schema.Tuple(/*#__PURE__*/Schema.Literal(9n), Credential.CDDLSchema, DRep.CDDLSchema),
/*#__PURE__*/
// 10: stake_vote_deleg_cert = (10, stake_credential, pool_keyhash, drep)
Schema.Tuple(/*#__PURE__*/Schema.Literal(10n), Credential.CDDLSchema, CBOR.ByteArray, DRep.CDDLSchema),
/*#__PURE__*/
// 11: stake_reg_deleg_cert = (11, stake_credential, pool_keyhash, coin)
Schema.Tuple(/*#__PURE__*/Schema.Literal(11n), Credential.CDDLSchema, CBOR.ByteArray, CBOR.Integer),
/*#__PURE__*/
// 12: vote_reg_deleg_cert = (12, stake_credential, drep, coin)
Schema.Tuple(/*#__PURE__*/Schema.Literal(12n), Credential.CDDLSchema, DRep.CDDLSchema, CBOR.Integer),
/*#__PURE__*/
// 13: stake_vote_reg_deleg_cert = (13, stake_credential, pool_keyhash, drep, coin)
Schema.Tuple(/*#__PURE__*/Schema.Literal(13n), Credential.CDDLSchema, CBOR.ByteArray, DRep.CDDLSchema, CBOR.Integer),
/*#__PURE__*/
// 14: auth_committee_hot_cert = (14, committee_cold_credential, committee_hot_credential)
Schema.Tuple(/*#__PURE__*/Schema.Literal(14n), Credential.CDDLSchema, Credential.CDDLSchema),
/*#__PURE__*/
// 15: resign_committee_cold_cert = (15, committee_cold_credential, anchor/ nil)
Schema.Tuple(/*#__PURE__*/Schema.Literal(15n), Credential.CDDLSchema, /*#__PURE__*/Schema.NullishOr(Anchor.CDDLSchema)),
/*#__PURE__*/
// 16: reg_drep_cert = (16, drep_credential, coin, anchor/ nil)
Schema.Tuple(/*#__PURE__*/Schema.Literal(16n), Credential.CDDLSchema, CBOR.Integer, /*#__PURE__*/Schema.NullishOr(Anchor.CDDLSchema)),
/*#__PURE__*/
// 17: unreg_drep_cert = (17, drep_credential, coin)
Schema.Tuple(/*#__PURE__*/Schema.Literal(17n), Credential.CDDLSchema, CBOR.Integer),
/*#__PURE__*/
// 18: update_drep_cert = (18, drep_credential, anchor/ nil)
Schema.Tuple(/*#__PURE__*/Schema.Literal(18n), Credential.CDDLSchema, /*#__PURE__*/Schema.NullishOr(Anchor.CDDLSchema)));
/**
 * CDDL schema for Certificate based on Conway specification.
 *
 * Transforms between CBOR tuple representation and Certificate union.
 * Each certificate type is encoded as [type_id, ...fields]
 *
 * @since 2.0.0
 * @category schemas
 */
export const FromCDDL = /*#__PURE__*/Schema.transformOrFail(CDDLSchema, /*#__PURE__*/Schema.typeSchema(Certificate), {
  strict: true,
  encode: toA => E.gen(function* () {
    switch (toA._tag) {
      case "StakeRegistration":
        {
          const credentialCDDL = yield* ParseResult.encodeEither(Credential.FromCDDL)(toA.stakeCredential);
          return [0n, credentialCDDL];
        }
      case "StakeDeregistration":
        {
          const credentialCDDL = yield* ParseResult.encodeEither(Credential.FromCDDL)(toA.stakeCredential);
          return [1n, credentialCDDL];
        }
      case "StakeDelegation":
        {
          const credentialCDDL = yield* ParseResult.encodeEither(Credential.FromCDDL)(toA.stakeCredential);
          const poolKeyHashBytes = yield* ParseResult.encodeEither(PoolKeyHash.FromBytes)(toA.poolKeyHash);
          return [2n, credentialCDDL, poolKeyHashBytes];
        }
      case "PoolRegistration":
        {
          const poolParamsCDDL = yield* ParseResult.encodeEither(PoolParams.FromCDDL)(toA.poolParams);
          // Spread encoded PoolParams fields directly into the certificate tuple (flattening)
          return [3n, ...poolParamsCDDL];
        }
      case "PoolRetirement":
        {
          const poolKeyHashBytes = yield* ParseResult.encodeEither(PoolKeyHash.FromBytes)(toA.poolKeyHash);
          return [4n, poolKeyHashBytes, BigInt(toA.epoch)];
        }
      case "RegCert":
        {
          const credentialCDDL = yield* ParseResult.encodeEither(Credential.FromCDDL)(toA.stakeCredential);
          return [7n, credentialCDDL, toA.coin];
        }
      case "UnregCert":
        {
          const credentialCDDL = yield* ParseResult.encodeEither(Credential.FromCDDL)(toA.stakeCredential);
          return [8n, credentialCDDL, toA.coin];
        }
      case "VoteDelegCert":
        {
          const credentialCDDL = yield* ParseResult.encodeEither(Credential.FromCDDL)(toA.stakeCredential);
          const drepCDDL = yield* ParseResult.encodeEither(DRep.FromCDDL)(toA.drep);
          return [9n, credentialCDDL, drepCDDL];
        }
      case "StakeVoteDelegCert":
        {
          const credentialCDDL = yield* ParseResult.encodeEither(Credential.FromCDDL)(toA.stakeCredential);
          const poolKeyHashBytes = yield* ParseResult.encodeEither(PoolKeyHash.FromBytes)(toA.poolKeyHash);
          const drepCDDL = yield* ParseResult.encodeEither(DRep.FromCDDL)(toA.drep);
          return [10n, credentialCDDL, poolKeyHashBytes, drepCDDL];
        }
      case "StakeRegDelegCert":
        {
          const credentialCDDL = yield* ParseResult.encodeEither(Credential.FromCDDL)(toA.stakeCredential);
          const poolKeyHashBytes = yield* ParseResult.encodeEither(PoolKeyHash.FromBytes)(toA.poolKeyHash);
          return [11n, credentialCDDL, poolKeyHashBytes, toA.coin];
        }
      case "VoteRegDelegCert":
        {
          const credentialCDDL = yield* ParseResult.encodeEither(Credential.FromCDDL)(toA.stakeCredential);
          const drepCDDL = yield* ParseResult.encodeEither(DRep.FromCDDL)(toA.drep);
          return [12n, credentialCDDL, drepCDDL, toA.coin];
        }
      case "StakeVoteRegDelegCert":
        {
          const credentialCDDL = yield* ParseResult.encodeEither(Credential.FromCDDL)(toA.stakeCredential);
          const poolKeyHashBytes = yield* ParseResult.encodeEither(PoolKeyHash.FromBytes)(toA.poolKeyHash);
          const drepCDDL = yield* ParseResult.encodeEither(DRep.FromCDDL)(toA.drep);
          return [13n, credentialCDDL, poolKeyHashBytes, drepCDDL, toA.coin];
        }
      case "AuthCommitteeHotCert":
        {
          const coldCredentialCDDL = yield* ParseResult.encodeEither(Credential.FromCDDL)(toA.committeeColdCredential);
          const hotCredentialCDDL = yield* ParseResult.encodeEither(Credential.FromCDDL)(toA.committeeHotCredential);
          return [14n, coldCredentialCDDL, hotCredentialCDDL];
        }
      case "ResignCommitteeColdCert":
        {
          const credentialCDDL = yield* ParseResult.encodeEither(Credential.FromCDDL)(toA.committeeColdCredential);
          const anchorCDDL = toA.anchor ? yield* ParseResult.encodeEither(Anchor.FromCDDL)(toA.anchor) : null;
          return [15n, credentialCDDL, anchorCDDL];
        }
      case "RegDrepCert":
        {
          const credentialCDDL = yield* ParseResult.encodeEither(Credential.FromCDDL)(toA.drepCredential);
          const anchorCDDL = toA.anchor ? yield* ParseResult.encodeEither(Anchor.FromCDDL)(toA.anchor) : null;
          return [16n, credentialCDDL, toA.coin, anchorCDDL];
        }
      case "UnregDrepCert":
        {
          const credentialCDDL = yield* ParseResult.encodeEither(Credential.FromCDDL)(toA.drepCredential);
          return [17n, credentialCDDL, toA.coin];
        }
      case "UpdateDrepCert":
        {
          const credentialCDDL = yield* ParseResult.encodeEither(Credential.FromCDDL)(toA.drepCredential);
          const anchorCDDL = toA.anchor ? yield* ParseResult.encodeEither(Anchor.FromCDDL)(toA.anchor) : null;
          return [18n, credentialCDDL, anchorCDDL];
        }
      // default:
      //   return yield* ParseResult.fail(
      //     new ParseResult.Type(CDDLSchema.ast, toA, `Unsupported certificate type: ${(toA as any)._tag}`)
      //   )
    }
  }),
  decode: fromA => E.gen(function* () {
    // const [typeId, ...fields] = fromA
    switch (fromA[0]) {
      case 0n:
        {
          // stake_registration = (0, stake_credential)
          // const [credentialCDDL] = fields
          const [, credentialCDDL] = fromA;
          const stakeCredential = yield* ParseResult.decodeEither(Credential.FromCDDL)(credentialCDDL);
          return new StakeRegistration({
            stakeCredential
          }, {
            disableValidation: true
          });
        }
      case 1n:
        {
          // stake_deregistration = (1, stake_credential)
          const [, credentialCDDL] = fromA;
          const stakeCredential = yield* ParseResult.decodeEither(Credential.FromCDDL)(credentialCDDL);
          return new StakeDeregistration({
            stakeCredential
          }, {
            disableValidation: true
          });
        }
      case 2n:
        {
          // stake_delegation = (2, stake_credential, pool_keyhash)
          const [, credentialCDDL, poolKeyHashBytes] = fromA;
          const stakeCredential = yield* ParseResult.decodeEither(Credential.FromCDDL)(credentialCDDL);
          const poolKeyHash = yield* ParseResult.decodeEither(PoolKeyHash.FromBytes)(poolKeyHashBytes);
          return new StakeDelegation({
            stakeCredential,
            poolKeyHash
          }, {
            disableValidation: true
          });
        }
      case 3n:
        {
          // pool_registration = (3, ...pool_params fields flattened)
          const [, operatorBytes, vrfKeyhashBytes, pledge, cost, marginEncoded, rewardAccountBytes, poolOwnersBytes, relaysEncoded, poolMetadataEncoded] = fromA;
          const poolParams = yield* ParseResult.decodeEither(PoolParams.FromCDDL)([operatorBytes, vrfKeyhashBytes, pledge, cost, marginEncoded, rewardAccountBytes, poolOwnersBytes, relaysEncoded, poolMetadataEncoded]);
          return new PoolRegistration({
            poolParams
          }, {
            disableValidation: true
          });
        }
      case 4n:
        {
          // pool_retirement = (4, pool_keyhash, epoch_no)
          const [, poolKeyHashBytes, epochBigInt] = fromA;
          const poolKeyHash = yield* ParseResult.decodeEither(PoolKeyHash.FromBytes)(poolKeyHashBytes);
          const epoch = epochBigInt;
          return new PoolRetirement({
            poolKeyHash,
            epoch
          }, {
            disableValidation: true
          });
        }
      case 7n:
        {
          // reg_cert = (7, stake_credential, coin)
          const [, credentialCDDL, coinBigInt] = fromA;
          const stakeCredential = yield* ParseResult.decodeEither(Credential.FromCDDL)(credentialCDDL);
          const coin = yield* ParseResult.decodeEither(Schema.typeSchema(Coin.Coin))(coinBigInt);
          return new RegCert({
            stakeCredential,
            coin
          }, {
            disableValidation: true
          });
        }
      case 8n:
        {
          // unreg_cert = (8, stake_credential, coin)
          const [, credentialCDDL, coinBigInt] = fromA;
          const stakeCredential = yield* ParseResult.decodeEither(Credential.FromCDDL)(credentialCDDL);
          const coin = yield* ParseResult.decodeEither(Schema.typeSchema(Coin.Coin))(coinBigInt);
          return new UnregCert({
            stakeCredential,
            coin
          }, {
            disableValidation: true
          });
        }
      case 9n:
        {
          // vote_deleg_cert = (9, stake_credential, drep)
          const [, credentialCDDL, drepCDDL] = fromA;
          const stakeCredential = yield* ParseResult.decodeEither(Credential.FromCDDL)(credentialCDDL);
          const drep = yield* ParseResult.decodeEither(DRep.FromCDDL)(drepCDDL);
          return new VoteDelegCert({
            stakeCredential,
            drep
          }, {
            disableValidation: true
          });
        }
      case 10n:
        {
          // stake_vote_deleg_cert = (10, stake_credential, pool_keyhash, drep)
          const [, credentialCDDL, poolKeyHashBytes, drepCDDL] = fromA;
          const stakeCredential = yield* ParseResult.decodeEither(Credential.FromCDDL)(credentialCDDL);
          const poolKeyHash = yield* ParseResult.decodeEither(PoolKeyHash.FromBytes)(poolKeyHashBytes);
          const drep = yield* ParseResult.decodeEither(DRep.FromCDDL)(drepCDDL);
          return new StakeVoteDelegCert({
            stakeCredential,
            poolKeyHash,
            drep
          }, {
            disableValidation: true
          });
        }
      case 11n:
        {
          // stake_reg_deleg_cert = (11, stake_credential, pool_keyhash, coin)
          const [, credentialCDDL, poolKeyHashBytes, coinBigInt] = fromA;
          const stakeCredential = yield* ParseResult.decodeEither(Credential.FromCDDL)(credentialCDDL);
          const poolKeyHash = yield* ParseResult.decodeEither(PoolKeyHash.FromBytes)(poolKeyHashBytes);
          const coin = yield* ParseResult.decodeEither(Schema.typeSchema(Coin.Coin))(coinBigInt);
          return new StakeRegDelegCert({
            stakeCredential,
            poolKeyHash,
            coin
          }, {
            disableValidation: true
          });
        }
      case 12n:
        {
          // vote_reg_deleg_cert = (12, stake_credential, drep, coin)
          const [, credentialCDDL, drepCDDL, coinBigInt] = fromA;
          const stakeCredential = yield* ParseResult.decodeEither(Credential.FromCDDL)(credentialCDDL);
          const drep = yield* ParseResult.decodeEither(DRep.FromCDDL)(drepCDDL);
          const coin = yield* ParseResult.decodeEither(Schema.typeSchema(Coin.Coin))(coinBigInt);
          return new VoteRegDelegCert({
            stakeCredential,
            drep,
            coin
          }, {
            disableValidation: true
          });
        }
      case 13n:
        {
          // stake_vote_reg_deleg_cert = (13, stake_credential, pool_keyhash, drep, coin)
          const [, credentialCDDL, poolKeyHashBytes, drepCDDL, coinBigInt] = fromA;
          const stakeCredential = yield* ParseResult.decodeEither(Credential.FromCDDL)(credentialCDDL);
          const poolKeyHash = yield* ParseResult.decodeEither(PoolKeyHash.FromBytes)(poolKeyHashBytes);
          const drep = yield* ParseResult.decodeEither(DRep.FromCDDL)(drepCDDL);
          const coin = yield* ParseResult.decodeEither(Schema.typeSchema(Coin.Coin))(coinBigInt);
          return new StakeVoteRegDelegCert({
            stakeCredential,
            poolKeyHash,
            drep,
            coin
          }, {
            disableValidation: true
          });
        }
      case 14n:
        {
          // auth_committee_hot_cert = (14, committee_cold_credential, committee_hot_credential)
          const [, coldCredentialCDDL, hotCredentialCDDL] = fromA;
          const committeeColdCredential = yield* ParseResult.decodeEither(Credential.FromCDDL)(coldCredentialCDDL);
          const committeeHotCredential = yield* ParseResult.decodeEither(Credential.FromCDDL)(hotCredentialCDDL);
          return new AuthCommitteeHotCert({
            committeeColdCredential,
            committeeHotCredential
          }, {
            disableValidation: true
          });
        }
      case 15n:
        {
          // resign_committee_cold_cert = (15, committee_cold_credential, anchor/ nil)
          const [, credentialCDDL, anchorCDDL] = fromA;
          const committeeColdCredential = yield* ParseResult.decodeEither(Credential.FromCDDL)(credentialCDDL);
          const anchor = anchorCDDL ? yield* ParseResult.decodeEither(Anchor.FromCDDL)(anchorCDDL) : undefined;
          return new ResignCommitteeColdCert({
            committeeColdCredential,
            anchor
          }, {
            disableValidation: true
          });
        }
      case 16n:
        {
          // reg_drep_cert = (16, drep_credential, coin, anchor/ nil)
          const [, credentialCDDL, coinBigInt, anchorCDDL] = fromA;
          const drepCredential = yield* ParseResult.decodeEither(Credential.FromCDDL)(credentialCDDL);
          const coin = yield* ParseResult.decodeEither(Schema.typeSchema(Coin.Coin))(coinBigInt);
          const anchor = anchorCDDL ? yield* ParseResult.decodeEither(Anchor.FromCDDL)(anchorCDDL) : undefined;
          return new RegDrepCert({
            drepCredential,
            coin,
            anchor
          }, {
            disableValidation: true
          });
        }
      case 17n:
        {
          // unreg_drep_cert = (17, drep_credential, coin)
          const [, credentialCDDL, coinBigInt] = fromA;
          const drepCredential = yield* ParseResult.decodeEither(Credential.FromCDDL)(credentialCDDL);
          const coin = yield* ParseResult.decodeEither(Schema.typeSchema(Coin.Coin))(coinBigInt);
          return new UnregDrepCert({
            drepCredential,
            coin
          }, {
            disableValidation: true
          });
        }
      case 18n:
        {
          // update_drep_cert = (18, drep_credential, anchor/ nil)
          const [, credentialCDDL, anchorCDDL] = fromA;
          const drepCredential = yield* ParseResult.decodeEither(Credential.FromCDDL)(credentialCDDL);
          const anchor = anchorCDDL ? yield* ParseResult.decodeEither(Anchor.FromCDDL)(anchorCDDL) : undefined;
          return new UpdateDrepCert({
            drepCredential,
            anchor
          }, {
            disableValidation: true
          });
        }
      // default:
      //   return yield* ParseResult.fail(
      //     new ParseResult.Type(CDDLSchema.ast, fromA, `Unsupported certificate type ID: ${fromA}`)
      //   )
    }
  })
});
/**
 * CBOR bytes transformation schema for Certificate.
 *
 * @since 2.0.0
 * @category schemas
 */
export const FromCBORBytes = (options = CBOR.CML_DEFAULT_OPTIONS) => Schema.compose(CBOR.FromBytes(options),
// Uint8Array → CBOR
FromCDDL // CBOR → Certificate
);
/**
 * CBOR hex transformation schema for Certificate.
 *
 * @since 2.0.0
 * @category schemas
 */
export const FromCBORHex = (options = CBOR.CML_DEFAULT_OPTIONS) => Schema.compose(Schema.Uint8ArrayFromHex,
// string → Uint8Array
FromCBORBytes(options) // Uint8Array → Certificate
);
/**
 * Check if the given value is a valid Certificate.
 *
 * @since 2.0.0
 * @category predicates
 */
export const is = /*#__PURE__*/Schema.is(Certificate);
/**
 * FastCheck arbitrary for Certificate instances.
 *
 * @since 2.0.0
 * @category testing
 */
export const arbitrary = /*#__PURE__*/FastCheck.oneof(
/*#__PURE__*/
// StakeRegistration
Credential.arbitrary.map(stakeCredential => new StakeRegistration({
  stakeCredential
})),
/*#__PURE__*/
// StakeDeregistration
Credential.arbitrary.map(stakeCredential => new StakeDeregistration({
  stakeCredential
})),
/*#__PURE__*/
// StakeDelegation
FastCheck.tuple(Credential.arbitrary, PoolKeyHash.arbitrary).map(([stakeCredential, poolKeyHash]) => new StakeDelegation({
  stakeCredential,
  poolKeyHash
})),
/*#__PURE__*/
// PoolRegistration
PoolParams.arbitrary.map(poolParams => new PoolRegistration({
  poolParams
})),
/*#__PURE__*/
// PoolRetirement
FastCheck.tuple(PoolKeyHash.arbitrary, EpochNo.generator).map(([poolKeyHash, epoch]) => new PoolRetirement({
  poolKeyHash,
  epoch: epoch
})),
/*#__PURE__*/
// RegCert
FastCheck.tuple(Credential.arbitrary, Coin.arbitrary).map(([stakeCredential, coin]) => new RegCert({
  stakeCredential,
  coin
})),
/*#__PURE__*/
// UnregCert
FastCheck.tuple(Credential.arbitrary, Coin.arbitrary).map(([stakeCredential, coin]) => new UnregCert({
  stakeCredential,
  coin
})),
/*#__PURE__*/
// VoteDelegCert
FastCheck.tuple(Credential.arbitrary, DRep.arbitrary).map(([stakeCredential, drep]) => new VoteDelegCert({
  stakeCredential,
  drep
})),
/*#__PURE__*/
// StakeVoteDelegCert
FastCheck.tuple(Credential.arbitrary, PoolKeyHash.arbitrary, DRep.arbitrary).map(([stakeCredential, poolKeyHash, drep]) => new StakeVoteDelegCert({
  stakeCredential,
  poolKeyHash,
  drep
})),
/*#__PURE__*/
// StakeRegDelegCert
FastCheck.tuple(Credential.arbitrary, PoolKeyHash.arbitrary, Coin.arbitrary).map(([stakeCredential, poolKeyHash, coin]) => new StakeRegDelegCert({
  stakeCredential,
  poolKeyHash,
  coin
})),
/*#__PURE__*/
// VoteRegDelegCert
FastCheck.tuple(Credential.arbitrary, DRep.arbitrary, Coin.arbitrary).map(([stakeCredential, drep, coin]) => new VoteRegDelegCert({
  stakeCredential,
  drep,
  coin
})),
/*#__PURE__*/
// StakeVoteRegDelegCert
FastCheck.tuple(Credential.arbitrary, PoolKeyHash.arbitrary, DRep.arbitrary, Coin.arbitrary).map(([stakeCredential, poolKeyHash, drep, coin]) => new StakeVoteRegDelegCert({
  stakeCredential,
  poolKeyHash,
  drep,
  coin
})),
/*#__PURE__*/
// AuthCommitteeHotCert
FastCheck.tuple(Credential.arbitrary, Credential.arbitrary).map(([committeeColdCredential, committeeHotCredential]) => new AuthCommitteeHotCert({
  committeeColdCredential,
  committeeHotCredential
})),
/*#__PURE__*/
// ResignCommitteeColdCert
FastCheck.tuple(Credential.arbitrary, FastCheck.option(Anchor.arbitrary, {
  nil: undefined
})).map(([committeeColdCredential, anchor]) => new ResignCommitteeColdCert({
  committeeColdCredential,
  anchor
})),
/*#__PURE__*/
// RegDrepCert
FastCheck.tuple(Credential.arbitrary, Coin.arbitrary, FastCheck.option(Anchor.arbitrary, {
  nil: undefined
})).map(([drepCredential, coin, anchor]) => new RegDrepCert({
  drepCredential,
  coin,
  anchor
})),
/*#__PURE__*/
// UnregDrepCert
FastCheck.tuple(Credential.arbitrary, Coin.arbitrary).map(([drepCredential, coin]) => new UnregDrepCert({
  drepCredential,
  coin
})),
/*#__PURE__*/
// UpdateDrepCert
FastCheck.tuple(Credential.arbitrary, FastCheck.option(Anchor.arbitrary, {
  nil: undefined
})).map(([drepCredential, anchor]) => new UpdateDrepCert({
  drepCredential,
  anchor
})));
/**
 * Parse a Certificate from CBOR bytes.
 *
 * @since 2.0.0
 * @category parsing
 */
export const fromCBORBytes = (bytes, options) => Schema.decodeSync(FromCBORBytes(options))(bytes);
/**
 * Parse a Certificate from CBOR hex string.
 *
 * @since 2.0.0
 * @category parsing
 */
export const fromCBORHex = (hex, options) => Schema.decodeSync(FromCBORHex(options))(hex);
/**
 * Convert a Certificate to CBOR bytes.
 *
 * @since 2.0.0
 * @category encoding
 */
export const toCBORBytes = (certificate, options) => Schema.encodeSync(FromCBORBytes(options))(certificate);
/**
 * Convert a Certificate to CBOR hex string.
 *
 * @since 2.0.0
 * @category encoding
 */
export const toCBORHex = (certificate, options) => Schema.encodeSync(FromCBORHex(options))(certificate);
//# sourceMappingURL=Certificate.js.map