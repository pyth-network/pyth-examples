/**
 * COSE Label types and constructors.
 *
 * @since 2.0.0
 * @category Message Signing
 */
import { Equal, Hash, Inspectable, Schema } from "effect";
/**
 * COSE Algorithm Identifiers (RFC 8152).
 *
 * @since 2.0.0
 * @category Enums
 */
export declare enum AlgorithmId {
    EdDSA = -8
}
/**
 * COSE Key Type values (RFC 8152).
 *
 * @since 2.0.0
 * @category Enums
 */
export declare enum KeyType {
    OKP = 1,
    EC2 = 2,
    RSA = 3,
    Symmetric = 4,
    HSS_LMS = 5,
    WalnutDSA = 6
}
/**
 * COSE Curve Type values (RFC 8152).
 *
 * @since 2.0.0
 * @category Enums
 */
export declare enum CurveType {
    P256 = 1,
    P384 = 2,
    P521 = 3,
    X25519 = 4,
    X448 = 5,
    Ed25519 = 6,
    Ed448 = 7,
    Secp256k1 = 8
}
/**
 * COSE Key Operations (RFC 8152).
 *
 * @since 2.0.0
 * @category Enums
 */
export declare enum KeyOperation {
    Sign = 0,
    Verify = 1,
    Encrypt = 2,
    Decrypt = 3,
    WrapKey = 4,
    UnwrapKey = 5,
    DeriveKey = 6,
    DeriveBits = 7,
    MacCreate = 8,
    MacVerify = 9
}
/**
 * Signature context for Sig_structure (RFC 8152).
 *
 * @since 2.0.0
 * @category Enums
 */
export declare enum SigContext {
    Signature = "Signature",
    Signature1 = "Signature1",
    CounterSignature = "CounterSignature"
}
/**
 * Label kind discriminator.
 *
 * @since 2.0.0
 * @category Enums
 */
export declare enum LabelKind {
    Int = 0,
    Text = 1
}
declare const Label_base: Schema.Class<Label, {
    kind: Schema.Enums<typeof LabelKind>;
    value: Schema.Union<[typeof Schema.BigIntFromSelf, typeof Schema.String]>;
}, Schema.Struct.Encoded<{
    kind: Schema.Enums<typeof LabelKind>;
    value: Schema.Union<[typeof Schema.BigIntFromSelf, typeof Schema.String]>;
}>, never, {
    readonly value: string | bigint;
} & {
    readonly kind: LabelKind;
}, {}, {}>;
/**
 * COSE header label - can be an integer or text string (RFC 8152).
 *
 * @since 2.0.0
 * @category Model
 */
export declare class Label extends Label_base {
    toJSON(): {
        _tag: "Label";
        kind: string;
        value: string | bigint;
    };
    toString(): string;
    [Inspectable.NodeInspectSymbol](): unknown;
    [Equal.symbol](that: unknown): boolean;
    [Hash.symbol](): number;
    /**
     * Get the integer value (throws if label is text).
     *
     * @since 2.0.0
     * @category Accessors
     */
    asInt(): bigint;
    /**
     * Get the text value (throws if label is integer).
     *
     * @since 2.0.0
     * @category Accessors
     */
    asText(): string;
}
/**
 * Create a Label from an integer.
 *
 * @since 2.0.0
 * @category Constructors
 */
export declare const labelFromInt: (value: bigint) => Label;
/**
 * Create a Label from a text string.
 *
 * @since 2.0.0
 * @category Constructors
 */
export declare const labelFromText: (value: string) => Label;
/**
 * Create a Label from AlgorithmId.
 *
 * @since 2.0.0
 * @category Constructors
 */
export declare const labelFromAlgorithmId: (alg: AlgorithmId) => Label;
/**
 * Create a Label from KeyType.
 *
 * @since 2.0.0
 * @category Constructors
 */
export declare const labelFromKeyType: (kty: KeyType) => Label;
/**
 * Create a Label from CurveType.
 *
 * @since 2.0.0
 * @category Constructors
 */
export declare const labelFromCurveType: (crv: CurveType) => Label;
export {};
//# sourceMappingURL=Label.d.ts.map