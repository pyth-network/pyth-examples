import { Schema } from "effect";
/**
 * Schema for Network representing Cardano network types.
 * Supports Mainnet, Preview, Preprod, and Custom networks.
 *
 * @since 2.0.0
 * @category schemas
 */
export const Network = /*#__PURE__*/Schema.Literal("Mainnet", "Preview", "Preprod", "Custom");
/**
 * Check if a value is a valid Network.
 *
 * @since 2.0.0
 * @category predicates
 */
export const is = value => Schema.is(Network)(value);
/**
 * Converts a Network type to NetworkId number.
 *
 * @since 2.0.0
 * @category conversion
 */
export const toId = network => {
  switch (network) {
    case "Preview":
    case "Preprod":
    case "Custom":
      return 0;
    case "Mainnet":
      return 1;
    default:
      throw new Error(`Exhaustive check failed: Unhandled case ${network}`);
  }
};
/**
 * Converts a NetworkId to Network type.
 *
 * @since 2.0.0
 * @category conversion
 */
export const fromId = id => {
  switch (id) {
    case 0:
      return "Preview";
    case 1:
      return "Mainnet";
    default:
      throw new Error(`Exhaustive check failed: Unhandled case ${id}`);
  }
};
//# sourceMappingURL=Network.js.map