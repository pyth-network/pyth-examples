import { Schema } from "effect";
import * as Header from "./Header.js";
import * as TransactionBody from "./TransactionBody.js";
declare const BlockClass_base: Schema.TaggedClass<BlockClass, "Block", {
    readonly _tag: Schema.tag<"Block">;
} & {
    header: typeof Header.Header;
    transactionBodies: Schema.Array$<typeof TransactionBody.TransactionBody>;
    invalidTransactions: Schema.Array$<Schema.refine<bigint, typeof Schema.BigInt>>;
}>;
/**
 * Block based on Conway CDDL specification
 *
 * ```
 * CDDL: block =
 *   [ header
 *   , transaction_bodies       : [* transaction_body]
 *   , transaction_witness_sets : [* transaction_witness_set]
 *   , auxiliary_data_set       : {* transaction_index => auxiliary_data}
 *   , invalid_transactions     : [* transaction_index]
 *   ]
 * ```
 *
 * Valid blocks must also satisfy the following two constraints:
 * 1. the length of transaction_bodies and transaction_witness_sets
 *    must be the same
 * 2. every transaction_index must be strictly smaller than the
 *    length of transaction_bodies
 *
 * @since 2.0.0
 * @category model
 */
export declare class BlockClass extends BlockClass_base {
}
export type Block = Schema.Schema.Type<typeof BlockClass>;
export {};
//# sourceMappingURL=Block.d.ts.map