import { FastCheck, Schema } from "effect";
/**
 * Reward address format schema (human-readable addresses)
 * Following CIP-0019 encoding requirements
 *
 * @since 2.0.0
 * @category schemas
 */
export const RewardAddress = /*#__PURE__*/Schema.String.pipe(Schema.pattern(/^(stake|stake_test)[1][a-z0-9]+$/i), Schema.brand("RewardAddress")).annotations({
  identifier: "RewardAddress"
});
/**
 * Check if the given value is a valid RewardAddress
 *
 * @since 2.0.0
 * @category predicates
 */
export const isRewardAddress = /*#__PURE__*/Schema.is(RewardAddress);
/**
 * FastCheck arbitrary for generating random RewardAddress instances.
 *
 * @since 2.0.0
 * @category arbitrary
 */
export const arbitrary = /*#__PURE__*/FastCheck.string({
  minLength: 50,
  maxLength: 100
}).filter(str => /^(stake|stake_test)[1][a-z0-9]+$/i.test(str)).map(str => RewardAddress.make(str));
//# sourceMappingURL=RewardAddress.js.map