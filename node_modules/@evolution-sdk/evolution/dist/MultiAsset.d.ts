import { Equal, FastCheck, Hash, Inspectable, Schema } from "effect";
import * as AssetName from "./AssetName.js";
import * as CBOR from "./CBOR.js";
import * as PolicyId from "./PolicyId.js";
/**
 * Schema for inner asset map (asset_name => bigint).
 *
 * This is a **base type** with no constraints. Values can be positive,
 * negative, or zero to support arithmetic operations.
 *
 * Constraints (positive values) are applied at boundaries like
 * `TransactionOutput` where CDDL requires positive_coin.
 *
 * @since 2.0.0
 * @category schemas
 */
export declare const AssetMap: Schema.transform<Schema.Array$<Schema.Tuple2<typeof AssetName.AssetName, typeof Schema.BigInt>>, Schema.MapFromSelf<Schema.SchemaClass<AssetName.AssetName, AssetName.AssetName, never>, Schema.SchemaClass<bigint, bigint, never>>>;
/**
 * Type alias for the inner asset map.
 *
 * @since 2.0.0
 * @category model
 */
export type AssetMap = typeof AssetMap.Type;
declare const MultiAsset_base: Schema.Class<MultiAsset, {
    map: Schema.Map$<typeof PolicyId.PolicyId, Schema.transform<Schema.Array$<Schema.Tuple2<typeof AssetName.AssetName, typeof Schema.BigInt>>, Schema.MapFromSelf<Schema.SchemaClass<AssetName.AssetName, AssetName.AssetName, never>, Schema.SchemaClass<bigint, bigint, never>>>>;
}, Schema.Struct.Encoded<{
    map: Schema.Map$<typeof PolicyId.PolicyId, Schema.transform<Schema.Array$<Schema.Tuple2<typeof AssetName.AssetName, typeof Schema.BigInt>>, Schema.MapFromSelf<Schema.SchemaClass<AssetName.AssetName, AssetName.AssetName, never>, Schema.SchemaClass<bigint, bigint, never>>>>;
}>, never, {
    readonly map: Map<PolicyId.PolicyId, Map<AssetName.AssetName, bigint>>;
}, {}, {}>;
/**
 * Schema for MultiAsset representing native assets.
 *
 * This is a **base type** with no constraints on size or values.
 * It supports arithmetic operations (merge, subtract, negate) which may
 * produce empty maps or negative quantities during intermediate calculations.
 *
 * Constraints (non-empty, positive values) are applied at boundaries
 * like `TransactionOutput` where CDDL requires `multiasset<positive_coin>`.
 *
 * ```
 * multiasset<a0> = {* policy_id => {* asset_name => a0}}
 * ```
 *
 * @since 2.0.0
 * @category model
 */
export declare class MultiAsset extends MultiAsset_base {
    /**
     * Convert to JSON representation.
     *
     * @since 2.0.0
     * @category conversions
     */
    toJSON(): {
        _tag: "MultiAsset";
        map: Record<string, Record<string, string>>;
    };
    /**
     * Convert to string representation.
     *
     * @since 2.0.0
     * @category conversions
     */
    toString(): string;
    /**
     * Custom inspect for Node.js REPL.
     *
     * @since 2.0.0
     * @category conversions
     */
    [Inspectable.NodeInspectSymbol](): unknown;
    /**
     * Structural equality check.
     *
     * @since 2.0.0
     * @category equality
     */
    [Equal.symbol](that: unknown): boolean;
    /**
     * @since 2.0.0
     * @category hashing
     */
    [Hash.symbol](): number;
}
export declare const equals: (a: MultiAsset, b: MultiAsset) => boolean;
/**
 * Create an empty MultiAsset
 *
 * @since 2.0.0
 * @category constructors
 */
export declare const empty: () => MultiAsset;
/**
 * Create a MultiAsset from a single asset.
 *
 * @since 2.0.0
 * @category constructors
 */
export declare const singleton: (policyId: PolicyId.PolicyId, assetName: AssetName.AssetName, amount: bigint) => MultiAsset;
/**
 * Add an asset to a MultiAsset, combining amounts if the asset already exists.
 *
 * @since 2.0.0
 * @category transformation
 */
export declare const addAsset: (multiAsset: MultiAsset, policyId: PolicyId.PolicyId, assetName: AssetName.AssetName, amount: bigint) => MultiAsset;
/**
 * Get the amount of a specific asset from a MultiAsset.
 *
 * @since 2.0.0
 * @category transformation
 */
export declare const getAsset: (multiAsset: MultiAsset, policyId: PolicyId.PolicyId, assetName: AssetName.AssetName) => bigint | undefined;
/**
 * Check if a MultiAsset contains a specific asset.
 *
 * @since 2.0.0
 * @category predicates
 */
export declare const hasAsset: (multiAsset: MultiAsset, policyId: PolicyId.PolicyId, assetName: AssetName.AssetName) => boolean;
/**
 * Get all policy IDs in a MultiAsset.
 *
 * @since 2.0.0
 * @category transformation
 */
export declare const getPolicyIds: (multiAsset: MultiAsset) => Array<PolicyId.PolicyId>;
/**
 * Get all assets for a specific policy ID.
 *
 * @since 2.0.0
 * @category transformation
 */
export declare const getAssetsByPolicy: (multiAsset: MultiAsset, policyId: PolicyId.PolicyId) => [AssetName.AssetName, bigint][];
/**
 * Check if a value is a valid MultiAsset.
 *
 * @since 2.0.0
 * @category predicates
 */
export declare const is: (value: unknown) => value is MultiAsset;
/**
 * Change generator to arbitrary and rename CBOR schemas.
 *
 * @since 2.0.0
 * @category arbitrary
 */
export declare const arbitrary: FastCheck.Arbitrary<MultiAsset>;
/**
 * CDDL schema for MultiAsset.
 *
 * ```
 * multiasset<positive_coin> = {+ policy_id => {+ asset_name => positive_coin}}
 * ```
 *
 * @since 2.0.0
 * @category schemas
 */
export declare const FromCDDL: Schema.transformOrFail<Schema.MapFromSelf<typeof Schema.Uint8ArrayFromSelf, Schema.MapFromSelf<typeof Schema.Uint8ArrayFromSelf, typeof Schema.BigIntFromSelf>>, Schema.SchemaClass<MultiAsset, MultiAsset, never>, never>;
/**
 * CBOR bytes transformation schema for MultiAsset.
 * Transforms between CBOR bytes and MultiAsset using CBOR encoding.
 *
 * @since 2.0.0
 * @category schemas
 */
export declare const FromCBORBytes: (options?: CBOR.CodecOptions) => Schema.transform<Schema.transformOrFail<typeof Schema.Uint8ArrayFromSelf, Schema.declare<CBOR.CBOR, CBOR.CBOR, readonly [], never>, never>, Schema.transformOrFail<Schema.MapFromSelf<typeof Schema.Uint8ArrayFromSelf, Schema.MapFromSelf<typeof Schema.Uint8ArrayFromSelf, typeof Schema.BigIntFromSelf>>, Schema.SchemaClass<MultiAsset, MultiAsset, never>, never>>;
/**
 * CBOR hex transformation schema for MultiAsset.
 * Transforms between CBOR hex string and MultiAsset using CBOR encoding.
 *
 * @since 2.0.0
 * @category schemas
 */
export declare const FromCBORHex: (options?: CBOR.CodecOptions) => Schema.transform<Schema.Schema<Uint8Array<ArrayBufferLike>, string, never>, Schema.transform<Schema.transformOrFail<typeof Schema.Uint8ArrayFromSelf, Schema.declare<CBOR.CBOR, CBOR.CBOR, readonly [], never>, never>, Schema.transformOrFail<Schema.MapFromSelf<typeof Schema.Uint8ArrayFromSelf, Schema.MapFromSelf<typeof Schema.Uint8ArrayFromSelf, typeof Schema.BigIntFromSelf>>, Schema.SchemaClass<MultiAsset, MultiAsset, never>, never>>>;
/**
 * Root Functions
 * ============================================================================
 */
/**
 * Parse MultiAsset from CBOR bytes.
 *
 * @since 2.0.0
 * @category parsing
 */
export declare const fromCBORBytes: (bytes: Uint8Array, options?: CBOR.CodecOptions) => MultiAsset;
/**
 * Parse MultiAsset from CBOR hex string.
 *
 * @since 2.0.0
 * @category parsing
 */
export declare const fromCBORHex: (hex: string, options?: CBOR.CodecOptions) => MultiAsset;
/**
 * Encode MultiAsset to CBOR bytes.
 *
 * @since 2.0.0
 * @category encoding
 */
export declare const toCBORBytes: (data: MultiAsset, options?: CBOR.CodecOptions) => Uint8Array<ArrayBufferLike>;
/**
 * Encode MultiAsset to CBOR hex string.
 *
 * @since 2.0.0
 * @category encoding
 */
export declare const toCBORHex: (data: MultiAsset, options?: CBOR.CodecOptions) => string;
/**
 * Merge two MultiAsset instances, combining amounts for assets that exist in both.
 *
 * @since 2.0.0
 * @category transformation
 */
export declare const merge: (a: MultiAsset, b: MultiAsset) => MultiAsset;
/**
 * Subtract MultiAsset b from MultiAsset a.
 * Returns a new MultiAsset with amounts reduced by the amounts in b.
 * If any asset would result in zero or negative amount, it's removed from the result.
 * If the result would be empty, an error is thrown since MultiAsset cannot be empty.
 *
 * @since 2.0.0
 * @category transformation
 */
export declare const subtract: (a: MultiAsset, b: MultiAsset) => MultiAsset;
export {};
//# sourceMappingURL=MultiAsset.d.ts.map