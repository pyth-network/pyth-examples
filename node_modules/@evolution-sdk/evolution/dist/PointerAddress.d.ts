import { Effect as Eff, Equal, FastCheck, Hash, Inspectable, ParseResult, Schema } from "effect";
import * as KeyHash from "./KeyHash.js";
import * as Natural from "./Natural.js";
import * as Pointer from "./Pointer.js";
import * as ScriptHash from "./ScriptHash.js";
declare const PointerAddress_base: Schema.TaggedClass<PointerAddress, "PointerAddress", {
    readonly _tag: Schema.tag<"PointerAddress">;
} & {
    networkId: Schema.refine<number, typeof Schema.NonNegative>;
    paymentCredential: Schema.Union<[typeof KeyHash.KeyHash, typeof ScriptHash.ScriptHash]>;
    pointer: typeof Pointer.Pointer;
}>;
/**
 * Pointer address with payment credential and pointer to stake registration
 *
 * @since 2.0.0
 * @category schemas
 */
export declare class PointerAddress extends PointerAddress_base {
    toJSON(): {
        _tag: "PointerAddress";
        networkId: number;
        paymentCredential: KeyHash.KeyHash | ScriptHash.ScriptHash;
        pointer: Pointer.Pointer;
    };
    toString(): string;
    [Inspectable.NodeInspectSymbol](): unknown;
    [Equal.symbol](that: unknown): boolean;
    [Hash.symbol](): number;
}
/**
 * Encode a positive integer using Cardano pointer varint (LEB128-like) encoding.
 * - Little-endian base-128: emit 7-bit groups, low to high, set MSB (0x80) on all but last.
 * - Matches decodeVariableLength below and Cardano pointer address spec.
 *
 * @since 2.0.0
 * @category encoding/decoding
 */
export declare const encodeVariableLength: (natural: Natural.Natural) => Eff.Effect<Uint8Array<ArrayBuffer>, ParseResult.ParseIssue, never>;
/**
 * Decode a variable length integer from a Uint8Array (LEB128-like)
 * Following the Cardano ledger implementation for variable-length integers
 *
 * @since 2.0.0
 * @category encoding/decoding
 */
export declare const decodeVariableLength: (bytes: Uint8Array, offset?: number | undefined) => Eff.Effect<[Natural.Natural, number], ParseResult.ParseIssue>;
export declare const FromBytes: Schema.transformOrFail<typeof Schema.Uint8ArrayFromSelf, Schema.SchemaClass<PointerAddress, PointerAddress, never>, never>;
export declare const FromHex: Schema.transform<Schema.Schema<Uint8Array<ArrayBufferLike>, string, never>, Schema.transformOrFail<typeof Schema.Uint8ArrayFromSelf, Schema.SchemaClass<PointerAddress, PointerAddress, never>, never>>;
/**
 * Smart constructor for creating PointerAddress instances
 *
/**
 * FastCheck arbitrary for generating random PointerAddress instances
 *
 * @since 2.0.0
 * @category arbitrary
 */
export declare const arbitrary: FastCheck.Arbitrary<PointerAddress>;
/**
 * Parse a PointerAddress from bytes.
 *
 * @since 2.0.0
 * @category parsing
 */
export declare const fromBytes: (bytes: Uint8Array) => PointerAddress;
/**
 * Parse a PointerAddress from hex string.
 *
 * @since 2.0.0
 * @category parsing
 */
export declare const fromHex: (hex: string) => PointerAddress;
/**
 * Convert a PointerAddress to bytes.
 *
 * @since 2.0.0
 * @category encoding
 */
export declare const toBytes: (data: PointerAddress) => Uint8Array<ArrayBufferLike>;
/**
 * Convert a PointerAddress to hex string.
 *
 * @since 2.0.0
 * @category encoding
 */
export declare const toHex: (data: PointerAddress) => string;
export {};
//# sourceMappingURL=PointerAddress.d.ts.map