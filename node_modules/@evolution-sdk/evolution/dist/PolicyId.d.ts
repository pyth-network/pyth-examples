import { Equal, FastCheck, Hash, Inspectable, Schema } from "effect";
declare const PolicyId_base: Schema.TaggedClass<PolicyId, "PolicyId", {
    readonly _tag: Schema.tag<"PolicyId">;
} & {
    hash: Schema.filter<Schema.Schema<Uint8Array<ArrayBufferLike>, string, never>>;
}>;
/**
 * PolicyId as a TaggedClass representing a minting policy identifier.
 * A PolicyId is a script hash (hash28) that identifies a minting policy.
 *
 * Note: PolicyId is equivalent to ScriptHash as defined in the CDDL:
 * policy_id = script_hash
 * script_hash = hash28
 *
 * @since 2.0.0
 * @category model
 */
export declare class PolicyId extends PolicyId_base {
    toJSON(): {
        _tag: "PolicyId";
        hash: string;
    };
    toString(): string;
    [Inspectable.NodeInspectSymbol](): unknown;
    [Equal.symbol](that: unknown): boolean;
    [Hash.symbol](): number;
}
/**
 * Schema transformer from bytes to PolicyId.
 *
 * @since 2.0.0
 * @category schemas
 */
export declare const FromBytes: Schema.transform<Schema.SchemaClass<Uint8Array<ArrayBufferLike>, Uint8Array<ArrayBufferLike>, never>, Schema.SchemaClass<PolicyId, PolicyId, never>>;
/**
 * Schema transformer from hex string to PolicyId.
 *
 * @since 2.0.0
 * @category schemas
 */
export declare const FromHex: Schema.transform<Schema.filter<Schema.Schema<Uint8Array<ArrayBufferLike>, string, never>>, Schema.transform<Schema.SchemaClass<Uint8Array<ArrayBufferLike>, Uint8Array<ArrayBufferLike>, never>, Schema.SchemaClass<PolicyId, PolicyId, never>>>;
/**
 * Check if the given value is a valid PolicyId
 *
 * @since 2.0.0
 * @category predicates
 */
export declare const isPolicyId: (u: unknown, overrideOptions?: import("effect/SchemaAST").ParseOptions | number) => u is PolicyId;
/**
 * FastCheck arbitrary for generating random PolicyId instances.
 *
 * @since 2.0.0
 * @category arbitrary
 */
export declare const arbitrary: FastCheck.Arbitrary<PolicyId>;
/**
 * Parse PolicyId from bytes.
 *
 * @since 2.0.0
 * @category parsing
 */
export declare const fromBytes: (i: Uint8Array<ArrayBufferLike>, overrideOptions?: import("effect/SchemaAST").ParseOptions) => PolicyId;
/**
 * Parse PolicyId from hex string.
 *
 * @since 2.0.0
 * @category parsing
 */
export declare const fromHex: (i: string, overrideOptions?: import("effect/SchemaAST").ParseOptions) => PolicyId;
/**
 * Encode PolicyId to bytes.
 *
 * @since 2.0.0
 * @category encoding
 */
export declare const toBytes: (a: PolicyId, overrideOptions?: import("effect/SchemaAST").ParseOptions) => Uint8Array<ArrayBufferLike>;
/**
 * Encode PolicyId to hex string.
 *
 * @since 2.0.0
 * @category encoding
 */
export declare const toHex: (a: PolicyId, overrideOptions?: import("effect/SchemaAST").ParseOptions) => string;
export {};
//# sourceMappingURL=PolicyId.d.ts.map