import { Schema } from "effect";
export declare const equals: (a: Uint8Array, b: Uint8Array) => boolean;
/**
 * Creates a curried filter that validates exact byte length (for Uint8Array).
 * Preserves Context inference from the base schema.
 *
 * @since 2.0.0
 * @category composition
 */
export declare const bytesLengthEquals: (byteLength: number) => <S extends Schema.Schema<any, any>>(baseSchema: S) => Schema.filter<S>;
/**
 * Creates a curried filter that validates byte length is within a range (for Uint8Array).
 * Preserves Context inference from the base schema.
 *
 * @since 2.0.0
 * @category composition
 */
export declare const bytesLengthBetween: (minBytes: number, maxBytes: number) => <S extends Schema.Schema<any, any>>(baseSchema: S) => Schema.filter<S>;
/**
 * Creates a curried filter that validates minimum byte length (for Uint8Array).
 * Preserves Context inference from the base schema.
 *
 * @since 2.0.0
 * @category composition
 */
export declare const bytesLengthMin: (minBytes: number) => <S extends Schema.Schema<any, Uint8Array>>(baseSchema: S) => Schema.filter<S>;
/**
 * Creates a curried filter that validates maximum byte length (for Uint8Array).
 * Preserves Context inference from the base schema.
 *
 * @since 2.0.0
 * @category composition
 */
export declare const bytesLengthMax: (maxBytes: number, moduleName: string) => <S extends Schema.Schema<any, Uint8Array>>(baseSchema: S) => Schema.filter<S>;
/**
 * Convert hex string to Uint8Array. Throws on invalid input.
 *
 * @since 2.0.0
 * @category conversions
 */
export declare const fromHex: (i: string, overrideOptions?: import("effect/SchemaAST").ParseOptions) => Uint8Array<ArrayBufferLike>;
/**
 * Convert Uint8Array to hex string. Never fails.
 *
 * @since 2.0.0
 * @category conversions
 */
export declare const toHex: (a: Uint8Array<ArrayBufferLike>, overrideOptions?: import("effect/SchemaAST").ParseOptions) => string;
//# sourceMappingURL=Bytes.d.ts.map