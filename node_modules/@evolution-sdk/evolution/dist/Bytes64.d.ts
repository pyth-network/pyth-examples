import { Schema } from "effect";
/**
 * Constant bytes length
 *
 * @since 2.0.0
 * @category constants
 */
export declare const BYTES_LENGTH = 64;
export declare const BytesFromHex: Schema.filter<Schema.Schema<Uint8Array<ArrayBufferLike>, string, never>>;
export declare const VariableBytesFromHex: Schema.filter<Schema.Schema<Uint8Array<ArrayBufferLike>, string, never>>;
/**
 * Decode fixed-length hex into bytes.
 *
 * @since 2.0.0
 * @category decoding
 */
export declare const fromHex: (i: string, overrideOptions?: import("effect/SchemaAST").ParseOptions) => Uint8Array<ArrayBufferLike>;
/**
 * Encode fixed-length bytes to hex.
 *
 * @since 2.0.0
 * @category encoding
 */
export declare const toHex: (a: Uint8Array<ArrayBufferLike>, overrideOptions?: import("effect/SchemaAST").ParseOptions) => string;
/**
 * Decode variable-length hex (0..BYTES_LENGTH) into bytes.
 *
 * @since 2.0.0
 * @category decoding
 */
export declare const fromVariableHex: (i: string, overrideOptions?: import("effect/SchemaAST").ParseOptions) => Uint8Array<ArrayBufferLike>;
/**
 * Encode variable-length bytes (0..BYTES_LENGTH) to hex.
 *
 * @since 2.0.0
 * @category encoding
 */
export declare const toVariableHex: (a: Uint8Array<ArrayBufferLike>, overrideOptions?: import("effect/SchemaAST").ParseOptions) => string;
export declare const equals: (a: Uint8Array, b: Uint8Array) => boolean;
//# sourceMappingURL=Bytes64.d.ts.map