[package]
name = "pyth-lazer-stellar-example"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
pyth-lazer-stellar-sdk = "0.3"
soroban-sdk = { version = "26.1.0", features = ["alloc"] }

[profile.release]
overflow-checks = true
